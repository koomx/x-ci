name: x-ci-vcpkg

on:
  workflow_call:
    inputs:
      config-command:
        description: 'CMake configure command'
        required: true
        type: string
      build-command:
        description: 'Build command'
        required: true
        type: string
      test-command:
        description: 'Test command (optional)'
        required: false
        type: string
        default: ''
      benchmark-command:
        description: 'Benchmark command (optional; skipped if empty)'
        required: false
        type: string
        default: ''
      plot-command:
        description: 'Plot command after benchmark (optional; skipped if empty)'
        required: false
        type: string
        default: ''
      benchmark-artifact-path:
        description: 'Directory to upload / publish'
        required: false
        type: string
        default: 'benchmark-results/'
      benchmark-publish-branch:
        description: 'If set, publish results to this branch under latest/<job-id>/'
        required: false
        type: string
        default: ''
      benchmark-publish-job:
        description: 'If set, only this job publishes; empty = every job publishes under latest/<job-id>/'
        required: false
        type: string
        default: ''
      deps-ubuntu:
        description: 'Extra apt packages (Ubuntu/Debian)'
        required: false
        type: string
        default: ''
      deps-alpine:
        description: 'Extra apk packages (Alpine)'
        required: false
        type: string
        default: ''
      deps-centos:
        description: 'Extra yum/dnf packages (CentOS)'
        required: false
        type: string
        default: ''
      deps-macos:
        description: 'Extra brew packages'
        required: false
        type: string
        default: ''
      deps-windows:
        description: 'Extra choco packages'
        required: false
        type: string
        default: ''
      enable-std17-ubuntu20-amd64:
        description: 'Enable job std17-ubuntu20-amd64'
        required: false
        type: boolean
        default: false
      enable-std17-ubuntu20-arm64:
        description: 'Enable job std17-ubuntu20-arm64'
        required: false
        type: boolean
        default: false
      enable-std17-debian11-amd64:
        description: 'Enable job std17-debian11-amd64'
        required: false
        type: boolean
        default: false
      enable-std17-debian11-arm64:
        description: 'Enable job std17-debian11-arm64'
        required: false
        type: boolean
        default: false
      enable-std17-centos7-amd64:
        description: 'Enable job std17-centos7-amd64'
        required: false
        type: boolean
        default: false
      enable-std17-centos7-arm64:
        description: 'Enable job std17-centos7-arm64'
        required: false
        type: boolean
        default: false
      enable-std20-ubuntu22-amd64:
        description: 'Enable job std20-ubuntu22-amd64'
        required: false
        type: boolean
        default: false
      enable-std20-ubuntu22-arm64:
        description: 'Enable job std20-ubuntu22-arm64'
        required: false
        type: boolean
        default: false
      enable-std20-ubuntu24-amd64:
        description: 'Enable job std20-ubuntu24-amd64'
        required: false
        type: boolean
        default: false
      enable-std20-ubuntu24-arm64:
        description: 'Enable job std20-ubuntu24-arm64'
        required: false
        type: boolean
        default: false
      enable-std20-debian12-amd64:
        description: 'Enable job std20-debian12-amd64'
        required: false
        type: boolean
        default: false
      enable-std20-debian12-arm64:
        description: 'Enable job std20-debian12-arm64'
        required: false
        type: boolean
        default: false
      enable-std20-alpine319-amd64:
        description: 'Enable job std20-alpine319-amd64'
        required: false
        type: boolean
        default: false
      enable-std20-alpine319-arm64:
        description: 'Enable job std20-alpine319-arm64'
        required: false
        type: boolean
        default: false
      enable-std20-alpine320-amd64:
        description: 'Enable job std20-alpine320-amd64'
        required: false
        type: boolean
        default: false
      enable-std20-alpine320-arm64:
        description: 'Enable job std20-alpine320-arm64'
        required: false
        type: boolean
        default: false
      enable-std20-centos9-amd64:
        description: 'Enable job std20-centos9-amd64'
        required: false
        type: boolean
        default: false
      enable-std20-centos9-arm64:
        description: 'Enable job std20-centos9-arm64'
        required: false
        type: boolean
        default: false
      enable-macos-arm64:
        description: 'Enable macos-arm64 job'
        required: false
        type: boolean
        default: false
      enable-macos-x86:
        description: 'Enable macos-x86 job'
        required: false
        type: boolean
        default: false
      enable-windows:
        description: 'Enable windows job'
        required: false
        type: boolean
        default: false
      enable-windows-arm64:
        description: 'Enable windows-arm64 job'
        required: false
        type: boolean
        default: false

jobs:

  std17-ubuntu20-amd64:
    if: ${{ inputs.enable-std17-ubuntu20-amd64 }}
    runs-on: ubuntu-latest
    container: ghcr.io/koomx/kumo-std17-ubuntu20-amd64:latest
    steps:
      - uses: actions/checkout@v4
      - name: Dump environment
        run: |
          echo "=== Environment Dump ==="
          if [ -f /etc/os-release ]; then
              . /etc/os-release
              echo "OS: $NAME $VERSION ($(uname -m))"
          elif [ "$(uname)" = "Darwin" ]; then
              echo "OS: macOS $(sw_vers -productVersion 2>/dev/null || echo unknown) ($(uname -m))"
          else
              echo "OS: $(uname -a 2>/dev/null || echo unknown)"
          fi
          for tool in cmake ninja make git python3 curl tar gzip; do
              if command -v $tool &>/dev/null; then
                  echo "  $tool: $(command -v $tool)  ($($tool --version 2>&1 | head -1))"
              else
                  echo "  $tool: NOT FOUND"
              fi
          done
          for cc in gcc g++ clang clang++ cc c++; do
              if command -v $cc &>/dev/null; then
                  echo "  $($cc --version 2>&1 | head -1)"
                  break
              fi
          done
          echo "=== End ==="
      - name: Install deps
        run: |
          if [ -n "${{ inputs.deps-ubuntu }}" ]; then
            apt-get update -qq
            apt-get install -y -qq ${{ inputs.deps-ubuntu }}
          fi
      - name: Bootstrap vcpkg
        run: |
          git clone --depth 1 --branch master https://github.com/microsoft/vcpkg.git /tmp/vcpkg
          /tmp/vcpkg/bootstrap-vcpkg.sh -disableMetrics
          echo "VCPKG_ROOT=/tmp/vcpkg" >> "$GITHUB_ENV"
          echo "VCPKG_CMAKE=/tmp/vcpkg/scripts/buildsystems/vcpkg.cmake" >> "$GITHUB_ENV"
          echo "VCPKG_FORCE_SYSTEM_BINARIES=1" >> "$GITHUB_ENV"
          mkdir -p "$RUNNER_TEMP/vcpkg-binary-cache"
          echo "VCPKG_DEFAULT_BINARY_CACHE=$RUNNER_TEMP/vcpkg-binary-cache" >> "$GITHUB_ENV"
      - name: Cache vcpkg binaries
        uses: actions/cache@v4
        with:
          path: ${{ runner.temp }}/vcpkg-binary-cache
          key: vcpkg-bin-std17-ubuntu20-amd64-${{ hashFiles('vcpkg.json', 'vcpkg-configuration.json', 'vcpkg-lock.json') }}
          restore-keys: |
            vcpkg-bin-std17-ubuntu20-amd64-
      - name: Configure
        run: ${{ inputs.config-command }}
      - name: Build
        run: ${{ inputs.build-command }}
      - name: Test
        if: ${{ inputs.test-command != '' }}
        run: ${{ inputs.test-command }}
      - name: Benchmark
        if: ${{ inputs.benchmark-command != '' }}
        run: ${{ inputs.benchmark-command }}
      - name: Plot
        if: ${{ inputs.plot-command != '' }}
        run: ${{ inputs.plot-command }}
      - name: Upload benchmark results
        if: ${{ (inputs.benchmark-command != '' || inputs.plot-command != '') && !cancelled() }}
        uses: actions/upload-artifact@v4
        with:
          name: std17-ubuntu20-amd64-benchmarks
          path: ${{ inputs.benchmark-artifact-path }}
          if-no-files-found: ignore
      - name: Publish benchmark results
        if: ${{ inputs.benchmark-publish-branch != '' && (inputs.benchmark-publish-job == '' || inputs.benchmark-publish-job == 'std17-ubuntu20-amd64') && success() && (github.event_name == 'push' || github.event_name == 'workflow_dispatch') }}
        shell: bash
        env:
          PUBLISH_BRANCH: ${{ inputs.benchmark-publish-branch }}
          SRC_DIR: ${{ inputs.benchmark-artifact-path }}
          GITHUB_TOKEN: ${{ github.token }}
          GITHUB_REPOSITORY: ${{ github.repository }}
        run: |
          set -euo pipefail
          if [ ! -d "$SRC_DIR" ]; then
            echo "No benchmark results at $SRC_DIR; skip publish"
            exit 0
          fi
          JOB_ID="${GITHUB_JOB}"
          ROOT="$GITHUB_WORKSPACE"
          cd "$ROOT"
          git config --global --add safe.directory "$ROOT"
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git remote set-url origin "https://x-access-token:${GITHUB_TOKEN}@github.com/${GITHUB_REPOSITORY}.git"
          WORK="$RUNNER_TEMP/bench-publish-$JOB_ID"
          cleanup_worktree() {
            cd "$ROOT"
            git worktree remove --force "$WORK" 2>/dev/null || true
            rm -rf "$WORK"
          }
          attempt=1
          while [ "$attempt" -le 12 ]; do
            echo "Publish attempt $attempt for $JOB_ID"
            cleanup_worktree
            mkdir -p "$WORK"
            if git fetch --depth 1 origin "$PUBLISH_BRANCH"; then
              git worktree add --force "$WORK" "FETCH_HEAD"
            else
              git worktree add --force --detach "$WORK"
              git -C "$WORK" checkout --orphan "$PUBLISH_BRANCH"
              git -C "$WORK" rm -rf . >/dev/null 2>&1 || true
            fi
            mkdir -p "$WORK/latest"
            rm -rf "$WORK/latest/$JOB_ID"
            mkdir -p "$WORK/latest/$JOB_ID"
            cp -a "$SRC_DIR"/. "$WORK/latest/$JOB_ID/"
            git -C "$WORK" add -A
            if git -C "$WORK" diff --cached --quiet; then
              echo "No benchmark publish changes for $JOB_ID"
              cleanup_worktree
              exit 0
            fi
            git -C "$WORK" commit -m "chore: update benchmark results for ${JOB_ID}"
            if git -C "$WORK" push origin "HEAD:$PUBLISH_BRANCH"; then
              echo "Published $JOB_ID to $PUBLISH_BRANCH/latest/$JOB_ID"
              cleanup_worktree
              exit 0
            fi
            echo "Push conflict; retrying..."
            cleanup_worktree
            attempt=$((attempt + 1))
            sleep $((attempt * 2))
          done
          echo "Failed to publish after retries"
          exit 1
      - name: Gather vcpkg logs
        if: failure()
        run: |
          mkdir -p build/vcpkg-logs
          if [ -d /tmp/vcpkg/buildtrees ]; then
            find /tmp/vcpkg/buildtrees -name "*.log" | while IFS= read -r f; do
              rel="${f#/tmp/vcpkg/}"
              mkdir -p "build/vcpkg-logs/$(dirname "$rel")"
              cp "$f" "build/vcpkg-logs/$rel"
            done
          fi
      - name: Upload logs on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: std17-ubuntu20-amd64-logs
          path: |
            build/CMakeFiles/CMakeOutput.log
            build/CMakeFiles/CMakeError.log
            build/Testing/Temporary/
            build/vcpkg-logs/
          if-no-files-found: ignore


  std17-ubuntu20-arm64:
    if: ${{ inputs.enable-std17-ubuntu20-arm64 }}
    runs-on: ubuntu-24.04-arm
    container: ghcr.io/koomx/kumo-std17-ubuntu20-arm64:latest
    steps:
      - uses: actions/checkout@v4
      - name: Dump environment
        run: |
          echo "=== Environment Dump ==="
          if [ -f /etc/os-release ]; then
              . /etc/os-release
              echo "OS: $NAME $VERSION ($(uname -m))"
          elif [ "$(uname)" = "Darwin" ]; then
              echo "OS: macOS $(sw_vers -productVersion 2>/dev/null || echo unknown) ($(uname -m))"
          else
              echo "OS: $(uname -a 2>/dev/null || echo unknown)"
          fi
          for tool in cmake ninja make git python3 curl tar gzip; do
              if command -v $tool &>/dev/null; then
                  echo "  $tool: $(command -v $tool)  ($($tool --version 2>&1 | head -1))"
              else
                  echo "  $tool: NOT FOUND"
              fi
          done
          for cc in gcc g++ clang clang++ cc c++; do
              if command -v $cc &>/dev/null; then
                  echo "  $($cc --version 2>&1 | head -1)"
                  break
              fi
          done
          echo "=== End ==="
      - name: Install deps
        run: |
          if [ -n "${{ inputs.deps-ubuntu }}" ]; then
            apt-get update -qq
            apt-get install -y -qq ${{ inputs.deps-ubuntu }}
          fi
      - name: Bootstrap vcpkg
        run: |
          git clone --depth 1 --branch master https://github.com/microsoft/vcpkg.git /tmp/vcpkg
          /tmp/vcpkg/bootstrap-vcpkg.sh -disableMetrics
          echo "VCPKG_ROOT=/tmp/vcpkg" >> "$GITHUB_ENV"
          echo "VCPKG_CMAKE=/tmp/vcpkg/scripts/buildsystems/vcpkg.cmake" >> "$GITHUB_ENV"
          echo "VCPKG_FORCE_SYSTEM_BINARIES=1" >> "$GITHUB_ENV"
          mkdir -p "$RUNNER_TEMP/vcpkg-binary-cache"
          echo "VCPKG_DEFAULT_BINARY_CACHE=$RUNNER_TEMP/vcpkg-binary-cache" >> "$GITHUB_ENV"
      - name: Cache vcpkg binaries
        uses: actions/cache@v4
        with:
          path: ${{ runner.temp }}/vcpkg-binary-cache
          key: vcpkg-bin-std17-ubuntu20-arm64-${{ hashFiles('vcpkg.json', 'vcpkg-configuration.json', 'vcpkg-lock.json') }}
          restore-keys: |
            vcpkg-bin-std17-ubuntu20-arm64-
      - name: Configure
        run: ${{ inputs.config-command }}
      - name: Build
        run: ${{ inputs.build-command }}
      - name: Test
        if: ${{ inputs.test-command != '' }}
        run: ${{ inputs.test-command }}
      - name: Benchmark
        if: ${{ inputs.benchmark-command != '' }}
        run: ${{ inputs.benchmark-command }}
      - name: Plot
        if: ${{ inputs.plot-command != '' }}
        run: ${{ inputs.plot-command }}
      - name: Upload benchmark results
        if: ${{ (inputs.benchmark-command != '' || inputs.plot-command != '') && !cancelled() }}
        uses: actions/upload-artifact@v4
        with:
          name: std17-ubuntu20-arm64-benchmarks
          path: ${{ inputs.benchmark-artifact-path }}
          if-no-files-found: ignore
      - name: Publish benchmark results
        if: ${{ inputs.benchmark-publish-branch != '' && (inputs.benchmark-publish-job == '' || inputs.benchmark-publish-job == 'std17-ubuntu20-arm64') && success() && (github.event_name == 'push' || github.event_name == 'workflow_dispatch') }}
        shell: bash
        env:
          PUBLISH_BRANCH: ${{ inputs.benchmark-publish-branch }}
          SRC_DIR: ${{ inputs.benchmark-artifact-path }}
          GITHUB_TOKEN: ${{ github.token }}
          GITHUB_REPOSITORY: ${{ github.repository }}
        run: |
          set -euo pipefail
          if [ ! -d "$SRC_DIR" ]; then
            echo "No benchmark results at $SRC_DIR; skip publish"
            exit 0
          fi
          JOB_ID="${GITHUB_JOB}"
          ROOT="$GITHUB_WORKSPACE"
          cd "$ROOT"
          git config --global --add safe.directory "$ROOT"
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git remote set-url origin "https://x-access-token:${GITHUB_TOKEN}@github.com/${GITHUB_REPOSITORY}.git"
          WORK="$RUNNER_TEMP/bench-publish-$JOB_ID"
          cleanup_worktree() {
            cd "$ROOT"
            git worktree remove --force "$WORK" 2>/dev/null || true
            rm -rf "$WORK"
          }
          attempt=1
          while [ "$attempt" -le 12 ]; do
            echo "Publish attempt $attempt for $JOB_ID"
            cleanup_worktree
            mkdir -p "$WORK"
            if git fetch --depth 1 origin "$PUBLISH_BRANCH"; then
              git worktree add --force "$WORK" "FETCH_HEAD"
            else
              git worktree add --force --detach "$WORK"
              git -C "$WORK" checkout --orphan "$PUBLISH_BRANCH"
              git -C "$WORK" rm -rf . >/dev/null 2>&1 || true
            fi
            mkdir -p "$WORK/latest"
            rm -rf "$WORK/latest/$JOB_ID"
            mkdir -p "$WORK/latest/$JOB_ID"
            cp -a "$SRC_DIR"/. "$WORK/latest/$JOB_ID/"
            git -C "$WORK" add -A
            if git -C "$WORK" diff --cached --quiet; then
              echo "No benchmark publish changes for $JOB_ID"
              cleanup_worktree
              exit 0
            fi
            git -C "$WORK" commit -m "chore: update benchmark results for ${JOB_ID}"
            if git -C "$WORK" push origin "HEAD:$PUBLISH_BRANCH"; then
              echo "Published $JOB_ID to $PUBLISH_BRANCH/latest/$JOB_ID"
              cleanup_worktree
              exit 0
            fi
            echo "Push conflict; retrying..."
            cleanup_worktree
            attempt=$((attempt + 1))
            sleep $((attempt * 2))
          done
          echo "Failed to publish after retries"
          exit 1
      - name: Gather vcpkg logs
        if: failure()
        run: |
          mkdir -p build/vcpkg-logs
          if [ -d /tmp/vcpkg/buildtrees ]; then
            find /tmp/vcpkg/buildtrees -name "*.log" | while IFS= read -r f; do
              rel="${f#/tmp/vcpkg/}"
              mkdir -p "build/vcpkg-logs/$(dirname "$rel")"
              cp "$f" "build/vcpkg-logs/$rel"
            done
          fi
      - name: Upload logs on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: std17-ubuntu20-arm64-logs
          path: |
            build/CMakeFiles/CMakeOutput.log
            build/CMakeFiles/CMakeError.log
            build/Testing/Temporary/
            build/vcpkg-logs/
          if-no-files-found: ignore


  std17-debian11-amd64:
    if: ${{ inputs.enable-std17-debian11-amd64 }}
    runs-on: ubuntu-latest
    container: ghcr.io/koomx/kumo-std17-debian11-amd64:latest
    steps:
      - uses: actions/checkout@v4
      - name: Dump environment
        run: |
          echo "=== Environment Dump ==="
          if [ -f /etc/os-release ]; then
              . /etc/os-release
              echo "OS: $NAME $VERSION ($(uname -m))"
          elif [ "$(uname)" = "Darwin" ]; then
              echo "OS: macOS $(sw_vers -productVersion 2>/dev/null || echo unknown) ($(uname -m))"
          else
              echo "OS: $(uname -a 2>/dev/null || echo unknown)"
          fi
          for tool in cmake ninja make git python3 curl tar gzip; do
              if command -v $tool &>/dev/null; then
                  echo "  $tool: $(command -v $tool)  ($($tool --version 2>&1 | head -1))"
              else
                  echo "  $tool: NOT FOUND"
              fi
          done
          for cc in gcc g++ clang clang++ cc c++; do
              if command -v $cc &>/dev/null; then
                  echo "  $($cc --version 2>&1 | head -1)"
                  break
              fi
          done
          echo "=== End ==="
      - name: Install deps
        run: |
          if [ -n "${{ inputs.deps-ubuntu }}" ]; then
            apt-get update -qq
            apt-get install -y -qq ${{ inputs.deps-ubuntu }}
          fi
      - name: Bootstrap vcpkg
        run: |
          git clone --depth 1 --branch master https://github.com/microsoft/vcpkg.git /tmp/vcpkg
          /tmp/vcpkg/bootstrap-vcpkg.sh -disableMetrics
          echo "VCPKG_ROOT=/tmp/vcpkg" >> "$GITHUB_ENV"
          echo "VCPKG_CMAKE=/tmp/vcpkg/scripts/buildsystems/vcpkg.cmake" >> "$GITHUB_ENV"
          echo "VCPKG_FORCE_SYSTEM_BINARIES=1" >> "$GITHUB_ENV"
          mkdir -p "$RUNNER_TEMP/vcpkg-binary-cache"
          echo "VCPKG_DEFAULT_BINARY_CACHE=$RUNNER_TEMP/vcpkg-binary-cache" >> "$GITHUB_ENV"
      - name: Cache vcpkg binaries
        uses: actions/cache@v4
        with:
          path: ${{ runner.temp }}/vcpkg-binary-cache
          key: vcpkg-bin-std17-debian11-amd64-${{ hashFiles('vcpkg.json', 'vcpkg-configuration.json', 'vcpkg-lock.json') }}
          restore-keys: |
            vcpkg-bin-std17-debian11-amd64-
      - name: Configure
        run: ${{ inputs.config-command }}
      - name: Build
        run: ${{ inputs.build-command }}
      - name: Test
        if: ${{ inputs.test-command != '' }}
        run: ${{ inputs.test-command }}
      - name: Benchmark
        if: ${{ inputs.benchmark-command != '' }}
        run: ${{ inputs.benchmark-command }}
      - name: Plot
        if: ${{ inputs.plot-command != '' }}
        run: ${{ inputs.plot-command }}
      - name: Upload benchmark results
        if: ${{ (inputs.benchmark-command != '' || inputs.plot-command != '') && !cancelled() }}
        uses: actions/upload-artifact@v4
        with:
          name: std17-debian11-amd64-benchmarks
          path: ${{ inputs.benchmark-artifact-path }}
          if-no-files-found: ignore
      - name: Publish benchmark results
        if: ${{ inputs.benchmark-publish-branch != '' && (inputs.benchmark-publish-job == '' || inputs.benchmark-publish-job == 'std17-debian11-amd64') && success() && (github.event_name == 'push' || github.event_name == 'workflow_dispatch') }}
        shell: bash
        env:
          PUBLISH_BRANCH: ${{ inputs.benchmark-publish-branch }}
          SRC_DIR: ${{ inputs.benchmark-artifact-path }}
          GITHUB_TOKEN: ${{ github.token }}
          GITHUB_REPOSITORY: ${{ github.repository }}
        run: |
          set -euo pipefail
          if [ ! -d "$SRC_DIR" ]; then
            echo "No benchmark results at $SRC_DIR; skip publish"
            exit 0
          fi
          JOB_ID="${GITHUB_JOB}"
          ROOT="$GITHUB_WORKSPACE"
          cd "$ROOT"
          git config --global --add safe.directory "$ROOT"
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git remote set-url origin "https://x-access-token:${GITHUB_TOKEN}@github.com/${GITHUB_REPOSITORY}.git"
          WORK="$RUNNER_TEMP/bench-publish-$JOB_ID"
          cleanup_worktree() {
            cd "$ROOT"
            git worktree remove --force "$WORK" 2>/dev/null || true
            rm -rf "$WORK"
          }
          attempt=1
          while [ "$attempt" -le 12 ]; do
            echo "Publish attempt $attempt for $JOB_ID"
            cleanup_worktree
            mkdir -p "$WORK"
            if git fetch --depth 1 origin "$PUBLISH_BRANCH"; then
              git worktree add --force "$WORK" "FETCH_HEAD"
            else
              git worktree add --force --detach "$WORK"
              git -C "$WORK" checkout --orphan "$PUBLISH_BRANCH"
              git -C "$WORK" rm -rf . >/dev/null 2>&1 || true
            fi
            mkdir -p "$WORK/latest"
            rm -rf "$WORK/latest/$JOB_ID"
            mkdir -p "$WORK/latest/$JOB_ID"
            cp -a "$SRC_DIR"/. "$WORK/latest/$JOB_ID/"
            git -C "$WORK" add -A
            if git -C "$WORK" diff --cached --quiet; then
              echo "No benchmark publish changes for $JOB_ID"
              cleanup_worktree
              exit 0
            fi
            git -C "$WORK" commit -m "chore: update benchmark results for ${JOB_ID}"
            if git -C "$WORK" push origin "HEAD:$PUBLISH_BRANCH"; then
              echo "Published $JOB_ID to $PUBLISH_BRANCH/latest/$JOB_ID"
              cleanup_worktree
              exit 0
            fi
            echo "Push conflict; retrying..."
            cleanup_worktree
            attempt=$((attempt + 1))
            sleep $((attempt * 2))
          done
          echo "Failed to publish after retries"
          exit 1
      - name: Gather vcpkg logs
        if: failure()
        run: |
          mkdir -p build/vcpkg-logs
          if [ -d /tmp/vcpkg/buildtrees ]; then
            find /tmp/vcpkg/buildtrees -name "*.log" | while IFS= read -r f; do
              rel="${f#/tmp/vcpkg/}"
              mkdir -p "build/vcpkg-logs/$(dirname "$rel")"
              cp "$f" "build/vcpkg-logs/$rel"
            done
          fi
      - name: Upload logs on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: std17-debian11-amd64-logs
          path: |
            build/CMakeFiles/CMakeOutput.log
            build/CMakeFiles/CMakeError.log
            build/Testing/Temporary/
            build/vcpkg-logs/
          if-no-files-found: ignore


  std17-debian11-arm64:
    if: ${{ inputs.enable-std17-debian11-arm64 }}
    runs-on: ubuntu-24.04-arm
    container: ghcr.io/koomx/kumo-std17-debian11-arm64:latest
    steps:
      - uses: actions/checkout@v4
      - name: Dump environment
        run: |
          echo "=== Environment Dump ==="
          if [ -f /etc/os-release ]; then
              . /etc/os-release
              echo "OS: $NAME $VERSION ($(uname -m))"
          elif [ "$(uname)" = "Darwin" ]; then
              echo "OS: macOS $(sw_vers -productVersion 2>/dev/null || echo unknown) ($(uname -m))"
          else
              echo "OS: $(uname -a 2>/dev/null || echo unknown)"
          fi
          for tool in cmake ninja make git python3 curl tar gzip; do
              if command -v $tool &>/dev/null; then
                  echo "  $tool: $(command -v $tool)  ($($tool --version 2>&1 | head -1))"
              else
                  echo "  $tool: NOT FOUND"
              fi
          done
          for cc in gcc g++ clang clang++ cc c++; do
              if command -v $cc &>/dev/null; then
                  echo "  $($cc --version 2>&1 | head -1)"
                  break
              fi
          done
          echo "=== End ==="
      - name: Install deps
        run: |
          if [ -n "${{ inputs.deps-ubuntu }}" ]; then
            apt-get update -qq
            apt-get install -y -qq ${{ inputs.deps-ubuntu }}
          fi
      - name: Bootstrap vcpkg
        run: |
          git clone --depth 1 --branch master https://github.com/microsoft/vcpkg.git /tmp/vcpkg
          /tmp/vcpkg/bootstrap-vcpkg.sh -disableMetrics
          echo "VCPKG_ROOT=/tmp/vcpkg" >> "$GITHUB_ENV"
          echo "VCPKG_CMAKE=/tmp/vcpkg/scripts/buildsystems/vcpkg.cmake" >> "$GITHUB_ENV"
          echo "VCPKG_FORCE_SYSTEM_BINARIES=1" >> "$GITHUB_ENV"
          mkdir -p "$RUNNER_TEMP/vcpkg-binary-cache"
          echo "VCPKG_DEFAULT_BINARY_CACHE=$RUNNER_TEMP/vcpkg-binary-cache" >> "$GITHUB_ENV"
      - name: Cache vcpkg binaries
        uses: actions/cache@v4
        with:
          path: ${{ runner.temp }}/vcpkg-binary-cache
          key: vcpkg-bin-std17-debian11-arm64-${{ hashFiles('vcpkg.json', 'vcpkg-configuration.json', 'vcpkg-lock.json') }}
          restore-keys: |
            vcpkg-bin-std17-debian11-arm64-
      - name: Configure
        run: ${{ inputs.config-command }}
      - name: Build
        run: ${{ inputs.build-command }}
      - name: Test
        if: ${{ inputs.test-command != '' }}
        run: ${{ inputs.test-command }}
      - name: Benchmark
        if: ${{ inputs.benchmark-command != '' }}
        run: ${{ inputs.benchmark-command }}
      - name: Plot
        if: ${{ inputs.plot-command != '' }}
        run: ${{ inputs.plot-command }}
      - name: Upload benchmark results
        if: ${{ (inputs.benchmark-command != '' || inputs.plot-command != '') && !cancelled() }}
        uses: actions/upload-artifact@v4
        with:
          name: std17-debian11-arm64-benchmarks
          path: ${{ inputs.benchmark-artifact-path }}
          if-no-files-found: ignore
      - name: Publish benchmark results
        if: ${{ inputs.benchmark-publish-branch != '' && (inputs.benchmark-publish-job == '' || inputs.benchmark-publish-job == 'std17-debian11-arm64') && success() && (github.event_name == 'push' || github.event_name == 'workflow_dispatch') }}
        shell: bash
        env:
          PUBLISH_BRANCH: ${{ inputs.benchmark-publish-branch }}
          SRC_DIR: ${{ inputs.benchmark-artifact-path }}
          GITHUB_TOKEN: ${{ github.token }}
          GITHUB_REPOSITORY: ${{ github.repository }}
        run: |
          set -euo pipefail
          if [ ! -d "$SRC_DIR" ]; then
            echo "No benchmark results at $SRC_DIR; skip publish"
            exit 0
          fi
          JOB_ID="${GITHUB_JOB}"
          ROOT="$GITHUB_WORKSPACE"
          cd "$ROOT"
          git config --global --add safe.directory "$ROOT"
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git remote set-url origin "https://x-access-token:${GITHUB_TOKEN}@github.com/${GITHUB_REPOSITORY}.git"
          WORK="$RUNNER_TEMP/bench-publish-$JOB_ID"
          cleanup_worktree() {
            cd "$ROOT"
            git worktree remove --force "$WORK" 2>/dev/null || true
            rm -rf "$WORK"
          }
          attempt=1
          while [ "$attempt" -le 12 ]; do
            echo "Publish attempt $attempt for $JOB_ID"
            cleanup_worktree
            mkdir -p "$WORK"
            if git fetch --depth 1 origin "$PUBLISH_BRANCH"; then
              git worktree add --force "$WORK" "FETCH_HEAD"
            else
              git worktree add --force --detach "$WORK"
              git -C "$WORK" checkout --orphan "$PUBLISH_BRANCH"
              git -C "$WORK" rm -rf . >/dev/null 2>&1 || true
            fi
            mkdir -p "$WORK/latest"
            rm -rf "$WORK/latest/$JOB_ID"
            mkdir -p "$WORK/latest/$JOB_ID"
            cp -a "$SRC_DIR"/. "$WORK/latest/$JOB_ID/"
            git -C "$WORK" add -A
            if git -C "$WORK" diff --cached --quiet; then
              echo "No benchmark publish changes for $JOB_ID"
              cleanup_worktree
              exit 0
            fi
            git -C "$WORK" commit -m "chore: update benchmark results for ${JOB_ID}"
            if git -C "$WORK" push origin "HEAD:$PUBLISH_BRANCH"; then
              echo "Published $JOB_ID to $PUBLISH_BRANCH/latest/$JOB_ID"
              cleanup_worktree
              exit 0
            fi
            echo "Push conflict; retrying..."
            cleanup_worktree
            attempt=$((attempt + 1))
            sleep $((attempt * 2))
          done
          echo "Failed to publish after retries"
          exit 1
      - name: Gather vcpkg logs
        if: failure()
        run: |
          mkdir -p build/vcpkg-logs
          if [ -d /tmp/vcpkg/buildtrees ]; then
            find /tmp/vcpkg/buildtrees -name "*.log" | while IFS= read -r f; do
              rel="${f#/tmp/vcpkg/}"
              mkdir -p "build/vcpkg-logs/$(dirname "$rel")"
              cp "$f" "build/vcpkg-logs/$rel"
            done
          fi
      - name: Upload logs on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: std17-debian11-arm64-logs
          path: |
            build/CMakeFiles/CMakeOutput.log
            build/CMakeFiles/CMakeError.log
            build/Testing/Temporary/
            build/vcpkg-logs/
          if-no-files-found: ignore


  std17-centos7-amd64:
    if: ${{ inputs.enable-std17-centos7-amd64 }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Dump environment
        shell: bash
        run: |
          SCRIPT="$(mktemp)"
          echo '' | base64 -d > "$SCRIPT"
          docker run --rm \
            -v "$SCRIPT:/tmp/xci-step.sh:ro" \
            -v "$GITHUB_WORKSPACE:$GITHUB_WORKSPACE" \
            -v "$RUNNER_TEMP:$RUNNER_TEMP" \
            -v "$GITHUB_ENV:$GITHUB_ENV" \
            -v "$GITHUB_PATH:$GITHUB_PATH" \
            -e GITHUB_ENV -e GITHUB_PATH -e RUNNER_TEMP -e GITHUB_WORKSPACE -e GITHUB_JOB \
            -e VCPKG_ROOT -e VCPKG_CMAKE -e VCPKG_DEFAULT_BINARY_CACHE -e VCPKG_FORCE_SYSTEM_BINARIES \
            -e KMPKG_HOME -e KMPKG_CMAKE -e KMPKG_FORCE_SYSTEM_BINARIES \
            -w "$GITHUB_WORKSPACE" \
            ghcr.io/koomx/kumo-std17-centos7-amd64:latest \
            sh /tmp/xci-step.sh
      - name: Install deps
        shell: bash
        env:
          XCI_DEPS: ${{ inputs.deps-centos }}
        run: |
          SCRIPT="$(mktemp)"
          echo 'aWYgWyAtbiAiJFhDSV9ERVBTIiBdOyB0aGVuCiAgeXVtIGluc3RhbGwgLXkgJFhDSV9ERVBTCmZpCg==' | base64 -d > "$SCRIPT"
          docker run --rm \
            -v "$SCRIPT:/tmp/xci-step.sh:ro" \
            -v "$GITHUB_WORKSPACE:$GITHUB_WORKSPACE" \
            -v "$RUNNER_TEMP:$RUNNER_TEMP" \
            -v "$GITHUB_ENV:$GITHUB_ENV" \
            -v "$GITHUB_PATH:$GITHUB_PATH" \
            -e GITHUB_ENV -e GITHUB_PATH -e RUNNER_TEMP -e GITHUB_WORKSPACE -e GITHUB_JOB \
            -e VCPKG_ROOT -e VCPKG_CMAKE -e VCPKG_DEFAULT_BINARY_CACHE -e VCPKG_FORCE_SYSTEM_BINARIES \
            -e KMPKG_HOME -e KMPKG_CMAKE -e KMPKG_FORCE_SYSTEM_BINARIES \
            -e XCI_DEPS \
            -w "$GITHUB_WORKSPACE" \
            ghcr.io/koomx/kumo-std17-centos7-amd64:latest \
            sh /tmp/xci-step.sh
      - name: Bootstrap vcpkg
        shell: bash
        run: |
          SCRIPT="$(mktemp)"
          echo '' | base64 -d > "$SCRIPT"
          docker run --rm \
            -v "$SCRIPT:/tmp/xci-step.sh:ro" \
            -v "$GITHUB_WORKSPACE:$GITHUB_WORKSPACE" \
            -v "$RUNNER_TEMP:$RUNNER_TEMP" \
            -v "$GITHUB_ENV:$GITHUB_ENV" \
            -v "$GITHUB_PATH:$GITHUB_PATH" \
            -e GITHUB_ENV -e GITHUB_PATH -e RUNNER_TEMP -e GITHUB_WORKSPACE -e GITHUB_JOB \
            -e VCPKG_ROOT -e VCPKG_CMAKE -e VCPKG_DEFAULT_BINARY_CACHE -e VCPKG_FORCE_SYSTEM_BINARIES \
            -e KMPKG_HOME -e KMPKG_CMAKE -e KMPKG_FORCE_SYSTEM_BINARIES \
            -w "$GITHUB_WORKSPACE" \
            ghcr.io/koomx/kumo-std17-centos7-amd64:latest \
            sh /tmp/xci-step.sh
      - name: Cache vcpkg binaries
        uses: actions/cache@v4
        with:
          path: ${{ runner.temp }}/vcpkg-binary-cache
          key: vcpkg-bin-std17-centos7-amd64-${{ hashFiles('vcpkg.json', 'vcpkg-configuration.json', 'vcpkg-lock.json') }}
          restore-keys: |
            vcpkg-bin-std17-centos7-amd64-
      - name: Configure
        shell: bash
        env:
          XCI_CMD: ${{ inputs.config-command }}
        run: |
          SCRIPT="$(mktemp)"
          printf '%s\n' "$XCI_CMD" > "$SCRIPT"
          docker run --rm \
            -v "$SCRIPT:/tmp/xci-step.sh:ro" \
            -v "$GITHUB_WORKSPACE:$GITHUB_WORKSPACE" \
            -v "$RUNNER_TEMP:$RUNNER_TEMP" \
            -v "$GITHUB_ENV:$GITHUB_ENV" \
            -v "$GITHUB_PATH:$GITHUB_PATH" \
            -e GITHUB_ENV -e GITHUB_PATH -e RUNNER_TEMP -e GITHUB_WORKSPACE -e GITHUB_JOB \
            -e VCPKG_ROOT -e VCPKG_CMAKE -e VCPKG_DEFAULT_BINARY_CACHE -e VCPKG_FORCE_SYSTEM_BINARIES \
            -e KMPKG_HOME -e KMPKG_CMAKE -e KMPKG_FORCE_SYSTEM_BINARIES \
            -w "$GITHUB_WORKSPACE" \
            ghcr.io/koomx/kumo-std17-centos7-amd64:latest \
            sh /tmp/xci-step.sh
      - name: Build
        shell: bash
        env:
          XCI_CMD: ${{ inputs.build-command }}
        run: |
          SCRIPT="$(mktemp)"
          printf '%s\n' "$XCI_CMD" > "$SCRIPT"
          docker run --rm \
            -v "$SCRIPT:/tmp/xci-step.sh:ro" \
            -v "$GITHUB_WORKSPACE:$GITHUB_WORKSPACE" \
            -v "$RUNNER_TEMP:$RUNNER_TEMP" \
            -v "$GITHUB_ENV:$GITHUB_ENV" \
            -v "$GITHUB_PATH:$GITHUB_PATH" \
            -e GITHUB_ENV -e GITHUB_PATH -e RUNNER_TEMP -e GITHUB_WORKSPACE -e GITHUB_JOB \
            -e VCPKG_ROOT -e VCPKG_CMAKE -e VCPKG_DEFAULT_BINARY_CACHE -e VCPKG_FORCE_SYSTEM_BINARIES \
            -e KMPKG_HOME -e KMPKG_CMAKE -e KMPKG_FORCE_SYSTEM_BINARIES \
            -w "$GITHUB_WORKSPACE" \
            ghcr.io/koomx/kumo-std17-centos7-amd64:latest \
            sh /tmp/xci-step.sh
      - name: Test
        if: ${{ inputs.test-command != '' }}
        shell: bash
        env:
          XCI_CMD: ${{ inputs.test-command }}
        run: |
          SCRIPT="$(mktemp)"
          printf '%s\n' "$XCI_CMD" > "$SCRIPT"
          docker run --rm \
            -v "$SCRIPT:/tmp/xci-step.sh:ro" \
            -v "$GITHUB_WORKSPACE:$GITHUB_WORKSPACE" \
            -v "$RUNNER_TEMP:$RUNNER_TEMP" \
            -v "$GITHUB_ENV:$GITHUB_ENV" \
            -v "$GITHUB_PATH:$GITHUB_PATH" \
            -e GITHUB_ENV -e GITHUB_PATH -e RUNNER_TEMP -e GITHUB_WORKSPACE -e GITHUB_JOB \
            -e VCPKG_ROOT -e VCPKG_CMAKE -e VCPKG_DEFAULT_BINARY_CACHE -e VCPKG_FORCE_SYSTEM_BINARIES \
            -e KMPKG_HOME -e KMPKG_CMAKE -e KMPKG_FORCE_SYSTEM_BINARIES \
            -w "$GITHUB_WORKSPACE" \
            ghcr.io/koomx/kumo-std17-centos7-amd64:latest \
            sh /tmp/xci-step.sh
      - name: Benchmark
        if: ${{ inputs.benchmark-command != '' }}
        shell: bash
        env:
          XCI_CMD: ${{ inputs.benchmark-command }}
        run: |
          SCRIPT="$(mktemp)"
          printf '%s\n' "$XCI_CMD" > "$SCRIPT"
          docker run --rm \
            -v "$SCRIPT:/tmp/xci-step.sh:ro" \
            -v "$GITHUB_WORKSPACE:$GITHUB_WORKSPACE" \
            -v "$RUNNER_TEMP:$RUNNER_TEMP" \
            -v "$GITHUB_ENV:$GITHUB_ENV" \
            -v "$GITHUB_PATH:$GITHUB_PATH" \
            -e GITHUB_ENV -e GITHUB_PATH -e RUNNER_TEMP -e GITHUB_WORKSPACE -e GITHUB_JOB \
            -e VCPKG_ROOT -e VCPKG_CMAKE -e VCPKG_DEFAULT_BINARY_CACHE -e VCPKG_FORCE_SYSTEM_BINARIES \
            -e KMPKG_HOME -e KMPKG_CMAKE -e KMPKG_FORCE_SYSTEM_BINARIES \
            -w "$GITHUB_WORKSPACE" \
            ghcr.io/koomx/kumo-std17-centos7-amd64:latest \
            sh /tmp/xci-step.sh
      - name: Plot
        if: ${{ inputs.plot-command != '' }}
        shell: bash
        env:
          XCI_CMD: ${{ inputs.plot-command }}
        run: |
          SCRIPT="$(mktemp)"
          printf '%s\n' "$XCI_CMD" > "$SCRIPT"
          docker run --rm \
            -v "$SCRIPT:/tmp/xci-step.sh:ro" \
            -v "$GITHUB_WORKSPACE:$GITHUB_WORKSPACE" \
            -v "$RUNNER_TEMP:$RUNNER_TEMP" \
            -v "$GITHUB_ENV:$GITHUB_ENV" \
            -v "$GITHUB_PATH:$GITHUB_PATH" \
            -e GITHUB_ENV -e GITHUB_PATH -e RUNNER_TEMP -e GITHUB_WORKSPACE -e GITHUB_JOB \
            -e VCPKG_ROOT -e VCPKG_CMAKE -e VCPKG_DEFAULT_BINARY_CACHE -e VCPKG_FORCE_SYSTEM_BINARIES \
            -e KMPKG_HOME -e KMPKG_CMAKE -e KMPKG_FORCE_SYSTEM_BINARIES \
            -w "$GITHUB_WORKSPACE" \
            ghcr.io/koomx/kumo-std17-centos7-amd64:latest \
            sh /tmp/xci-step.sh
      - name: Upload benchmark results
        if: ${{ (inputs.benchmark-command != '' || inputs.plot-command != '') && !cancelled() }}
        uses: actions/upload-artifact@v4
        with:
          name: std17-centos7-amd64-benchmarks
          path: ${{ inputs.benchmark-artifact-path }}
          if-no-files-found: ignore
      - name: Publish benchmark results
        if: ${{ inputs.benchmark-publish-branch != '' && (inputs.benchmark-publish-job == '' || inputs.benchmark-publish-job == 'std17-centos7-amd64') && success() && (github.event_name == 'push' || github.event_name == 'workflow_dispatch') }}
        shell: bash
        env:
          PUBLISH_BRANCH: ${{ inputs.benchmark-publish-branch }}
          SRC_DIR: ${{ inputs.benchmark-artifact-path }}
          GITHUB_TOKEN: ${{ github.token }}
          GITHUB_REPOSITORY: ${{ github.repository }}
        run: |
          set -euo pipefail
          if [ ! -d "$SRC_DIR" ]; then
            echo "No benchmark results at $SRC_DIR; skip publish"
            exit 0
          fi
          JOB_ID="${GITHUB_JOB}"
          ROOT="$GITHUB_WORKSPACE"
          cd "$ROOT"
          git config --global --add safe.directory "$ROOT"
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git remote set-url origin "https://x-access-token:${GITHUB_TOKEN}@github.com/${GITHUB_REPOSITORY}.git"
          WORK="$RUNNER_TEMP/bench-publish-$JOB_ID"
          cleanup_worktree() {
            cd "$ROOT"
            git worktree remove --force "$WORK" 2>/dev/null || true
            rm -rf "$WORK"
          }
          attempt=1
          while [ "$attempt" -le 12 ]; do
            echo "Publish attempt $attempt for $JOB_ID"
            cleanup_worktree
            mkdir -p "$WORK"
            if git fetch --depth 1 origin "$PUBLISH_BRANCH"; then
              git worktree add --force "$WORK" "FETCH_HEAD"
            else
              git worktree add --force --detach "$WORK"
              git -C "$WORK" checkout --orphan "$PUBLISH_BRANCH"
              git -C "$WORK" rm -rf . >/dev/null 2>&1 || true
            fi
            mkdir -p "$WORK/latest"
            rm -rf "$WORK/latest/$JOB_ID"
            mkdir -p "$WORK/latest/$JOB_ID"
            cp -a "$SRC_DIR"/. "$WORK/latest/$JOB_ID/"
            git -C "$WORK" add -A
            if git -C "$WORK" diff --cached --quiet; then
              echo "No benchmark publish changes for $JOB_ID"
              cleanup_worktree
              exit 0
            fi
            git -C "$WORK" commit -m "chore: update benchmark results for ${JOB_ID}"
            if git -C "$WORK" push origin "HEAD:$PUBLISH_BRANCH"; then
              echo "Published $JOB_ID to $PUBLISH_BRANCH/latest/$JOB_ID"
              cleanup_worktree
              exit 0
            fi
            echo "Push conflict; retrying..."
            cleanup_worktree
            attempt=$((attempt + 1))
            sleep $((attempt * 2))
          done
          echo "Failed to publish after retries"
          exit 1
      - name: Gather logs
        if: failure()
        shell: bash
        run: |
          SCRIPT="$(mktemp)"
          echo '' | base64 -d > "$SCRIPT"
          docker run --rm \
            -v "$SCRIPT:/tmp/xci-step.sh:ro" \
            -v "$GITHUB_WORKSPACE:$GITHUB_WORKSPACE" \
            -v "$RUNNER_TEMP:$RUNNER_TEMP" \
            -v "$GITHUB_ENV:$GITHUB_ENV" \
            -v "$GITHUB_PATH:$GITHUB_PATH" \
            -e GITHUB_ENV -e GITHUB_PATH -e RUNNER_TEMP -e GITHUB_WORKSPACE -e GITHUB_JOB \
            -e VCPKG_ROOT -e VCPKG_CMAKE -e VCPKG_DEFAULT_BINARY_CACHE -e VCPKG_FORCE_SYSTEM_BINARIES \
            -e KMPKG_HOME -e KMPKG_CMAKE -e KMPKG_FORCE_SYSTEM_BINARIES \
            -w "$GITHUB_WORKSPACE" \
            ghcr.io/koomx/kumo-std17-centos7-amd64:latest \
            sh /tmp/xci-step.sh
      - name: Upload logs on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: std17-centos7-amd64-logs
          path: |
            build/CMakeFiles/CMakeOutput.log
            build/CMakeFiles/CMakeError.log
            build/Testing/Temporary/
            build/vcpkg-logs/
          if-no-files-found: ignore

  std17-centos7-arm64:
    if: ${{ inputs.enable-std17-centos7-arm64 }}
    runs-on: ubuntu-24.04-arm
    steps:
      - uses: actions/checkout@v4
      - name: Dump environment
        shell: bash
        run: |
          SCRIPT="$(mktemp)"
          echo '' | base64 -d > "$SCRIPT"
          docker run --rm \
            -v "$SCRIPT:/tmp/xci-step.sh:ro" \
            -v "$GITHUB_WORKSPACE:$GITHUB_WORKSPACE" \
            -v "$RUNNER_TEMP:$RUNNER_TEMP" \
            -v "$GITHUB_ENV:$GITHUB_ENV" \
            -v "$GITHUB_PATH:$GITHUB_PATH" \
            -e GITHUB_ENV -e GITHUB_PATH -e RUNNER_TEMP -e GITHUB_WORKSPACE -e GITHUB_JOB \
            -e VCPKG_ROOT -e VCPKG_CMAKE -e VCPKG_DEFAULT_BINARY_CACHE -e VCPKG_FORCE_SYSTEM_BINARIES \
            -e KMPKG_HOME -e KMPKG_CMAKE -e KMPKG_FORCE_SYSTEM_BINARIES \
            -w "$GITHUB_WORKSPACE" \
            ghcr.io/koomx/kumo-std17-centos7-arm64:latest \
            sh /tmp/xci-step.sh
      - name: Install deps
        shell: bash
        env:
          XCI_DEPS: ${{ inputs.deps-centos }}
        run: |
          SCRIPT="$(mktemp)"
          echo 'aWYgWyAtbiAiJFhDSV9ERVBTIiBdOyB0aGVuCiAgeXVtIGluc3RhbGwgLXkgJFhDSV9ERVBTCmZpCg==' | base64 -d > "$SCRIPT"
          docker run --rm \
            -v "$SCRIPT:/tmp/xci-step.sh:ro" \
            -v "$GITHUB_WORKSPACE:$GITHUB_WORKSPACE" \
            -v "$RUNNER_TEMP:$RUNNER_TEMP" \
            -v "$GITHUB_ENV:$GITHUB_ENV" \
            -v "$GITHUB_PATH:$GITHUB_PATH" \
            -e GITHUB_ENV -e GITHUB_PATH -e RUNNER_TEMP -e GITHUB_WORKSPACE -e GITHUB_JOB \
            -e VCPKG_ROOT -e VCPKG_CMAKE -e VCPKG_DEFAULT_BINARY_CACHE -e VCPKG_FORCE_SYSTEM_BINARIES \
            -e KMPKG_HOME -e KMPKG_CMAKE -e KMPKG_FORCE_SYSTEM_BINARIES \
            -e XCI_DEPS \
            -w "$GITHUB_WORKSPACE" \
            ghcr.io/koomx/kumo-std17-centos7-arm64:latest \
            sh /tmp/xci-step.sh
      - name: Bootstrap vcpkg
        shell: bash
        run: |
          SCRIPT="$(mktemp)"
          echo '' | base64 -d > "$SCRIPT"
          docker run --rm \
            -v "$SCRIPT:/tmp/xci-step.sh:ro" \
            -v "$GITHUB_WORKSPACE:$GITHUB_WORKSPACE" \
            -v "$RUNNER_TEMP:$RUNNER_TEMP" \
            -v "$GITHUB_ENV:$GITHUB_ENV" \
            -v "$GITHUB_PATH:$GITHUB_PATH" \
            -e GITHUB_ENV -e GITHUB_PATH -e RUNNER_TEMP -e GITHUB_WORKSPACE -e GITHUB_JOB \
            -e VCPKG_ROOT -e VCPKG_CMAKE -e VCPKG_DEFAULT_BINARY_CACHE -e VCPKG_FORCE_SYSTEM_BINARIES \
            -e KMPKG_HOME -e KMPKG_CMAKE -e KMPKG_FORCE_SYSTEM_BINARIES \
            -w "$GITHUB_WORKSPACE" \
            ghcr.io/koomx/kumo-std17-centos7-arm64:latest \
            sh /tmp/xci-step.sh
      - name: Cache vcpkg binaries
        uses: actions/cache@v4
        with:
          path: ${{ runner.temp }}/vcpkg-binary-cache
          key: vcpkg-bin-std17-centos7-arm64-${{ hashFiles('vcpkg.json', 'vcpkg-configuration.json', 'vcpkg-lock.json') }}
          restore-keys: |
            vcpkg-bin-std17-centos7-arm64-
      - name: Configure
        shell: bash
        env:
          XCI_CMD: ${{ inputs.config-command }}
        run: |
          SCRIPT="$(mktemp)"
          printf '%s\n' "$XCI_CMD" > "$SCRIPT"
          docker run --rm \
            -v "$SCRIPT:/tmp/xci-step.sh:ro" \
            -v "$GITHUB_WORKSPACE:$GITHUB_WORKSPACE" \
            -v "$RUNNER_TEMP:$RUNNER_TEMP" \
            -v "$GITHUB_ENV:$GITHUB_ENV" \
            -v "$GITHUB_PATH:$GITHUB_PATH" \
            -e GITHUB_ENV -e GITHUB_PATH -e RUNNER_TEMP -e GITHUB_WORKSPACE -e GITHUB_JOB \
            -e VCPKG_ROOT -e VCPKG_CMAKE -e VCPKG_DEFAULT_BINARY_CACHE -e VCPKG_FORCE_SYSTEM_BINARIES \
            -e KMPKG_HOME -e KMPKG_CMAKE -e KMPKG_FORCE_SYSTEM_BINARIES \
            -w "$GITHUB_WORKSPACE" \
            ghcr.io/koomx/kumo-std17-centos7-arm64:latest \
            sh /tmp/xci-step.sh
      - name: Build
        shell: bash
        env:
          XCI_CMD: ${{ inputs.build-command }}
        run: |
          SCRIPT="$(mktemp)"
          printf '%s\n' "$XCI_CMD" > "$SCRIPT"
          docker run --rm \
            -v "$SCRIPT:/tmp/xci-step.sh:ro" \
            -v "$GITHUB_WORKSPACE:$GITHUB_WORKSPACE" \
            -v "$RUNNER_TEMP:$RUNNER_TEMP" \
            -v "$GITHUB_ENV:$GITHUB_ENV" \
            -v "$GITHUB_PATH:$GITHUB_PATH" \
            -e GITHUB_ENV -e GITHUB_PATH -e RUNNER_TEMP -e GITHUB_WORKSPACE -e GITHUB_JOB \
            -e VCPKG_ROOT -e VCPKG_CMAKE -e VCPKG_DEFAULT_BINARY_CACHE -e VCPKG_FORCE_SYSTEM_BINARIES \
            -e KMPKG_HOME -e KMPKG_CMAKE -e KMPKG_FORCE_SYSTEM_BINARIES \
            -w "$GITHUB_WORKSPACE" \
            ghcr.io/koomx/kumo-std17-centos7-arm64:latest \
            sh /tmp/xci-step.sh
      - name: Test
        if: ${{ inputs.test-command != '' }}
        shell: bash
        env:
          XCI_CMD: ${{ inputs.test-command }}
        run: |
          SCRIPT="$(mktemp)"
          printf '%s\n' "$XCI_CMD" > "$SCRIPT"
          docker run --rm \
            -v "$SCRIPT:/tmp/xci-step.sh:ro" \
            -v "$GITHUB_WORKSPACE:$GITHUB_WORKSPACE" \
            -v "$RUNNER_TEMP:$RUNNER_TEMP" \
            -v "$GITHUB_ENV:$GITHUB_ENV" \
            -v "$GITHUB_PATH:$GITHUB_PATH" \
            -e GITHUB_ENV -e GITHUB_PATH -e RUNNER_TEMP -e GITHUB_WORKSPACE -e GITHUB_JOB \
            -e VCPKG_ROOT -e VCPKG_CMAKE -e VCPKG_DEFAULT_BINARY_CACHE -e VCPKG_FORCE_SYSTEM_BINARIES \
            -e KMPKG_HOME -e KMPKG_CMAKE -e KMPKG_FORCE_SYSTEM_BINARIES \
            -w "$GITHUB_WORKSPACE" \
            ghcr.io/koomx/kumo-std17-centos7-arm64:latest \
            sh /tmp/xci-step.sh
      - name: Benchmark
        if: ${{ inputs.benchmark-command != '' }}
        shell: bash
        env:
          XCI_CMD: ${{ inputs.benchmark-command }}
        run: |
          SCRIPT="$(mktemp)"
          printf '%s\n' "$XCI_CMD" > "$SCRIPT"
          docker run --rm \
            -v "$SCRIPT:/tmp/xci-step.sh:ro" \
            -v "$GITHUB_WORKSPACE:$GITHUB_WORKSPACE" \
            -v "$RUNNER_TEMP:$RUNNER_TEMP" \
            -v "$GITHUB_ENV:$GITHUB_ENV" \
            -v "$GITHUB_PATH:$GITHUB_PATH" \
            -e GITHUB_ENV -e GITHUB_PATH -e RUNNER_TEMP -e GITHUB_WORKSPACE -e GITHUB_JOB \
            -e VCPKG_ROOT -e VCPKG_CMAKE -e VCPKG_DEFAULT_BINARY_CACHE -e VCPKG_FORCE_SYSTEM_BINARIES \
            -e KMPKG_HOME -e KMPKG_CMAKE -e KMPKG_FORCE_SYSTEM_BINARIES \
            -w "$GITHUB_WORKSPACE" \
            ghcr.io/koomx/kumo-std17-centos7-arm64:latest \
            sh /tmp/xci-step.sh
      - name: Plot
        if: ${{ inputs.plot-command != '' }}
        shell: bash
        env:
          XCI_CMD: ${{ inputs.plot-command }}
        run: |
          SCRIPT="$(mktemp)"
          printf '%s\n' "$XCI_CMD" > "$SCRIPT"
          docker run --rm \
            -v "$SCRIPT:/tmp/xci-step.sh:ro" \
            -v "$GITHUB_WORKSPACE:$GITHUB_WORKSPACE" \
            -v "$RUNNER_TEMP:$RUNNER_TEMP" \
            -v "$GITHUB_ENV:$GITHUB_ENV" \
            -v "$GITHUB_PATH:$GITHUB_PATH" \
            -e GITHUB_ENV -e GITHUB_PATH -e RUNNER_TEMP -e GITHUB_WORKSPACE -e GITHUB_JOB \
            -e VCPKG_ROOT -e VCPKG_CMAKE -e VCPKG_DEFAULT_BINARY_CACHE -e VCPKG_FORCE_SYSTEM_BINARIES \
            -e KMPKG_HOME -e KMPKG_CMAKE -e KMPKG_FORCE_SYSTEM_BINARIES \
            -w "$GITHUB_WORKSPACE" \
            ghcr.io/koomx/kumo-std17-centos7-arm64:latest \
            sh /tmp/xci-step.sh
      - name: Upload benchmark results
        if: ${{ (inputs.benchmark-command != '' || inputs.plot-command != '') && !cancelled() }}
        uses: actions/upload-artifact@v4
        with:
          name: std17-centos7-arm64-benchmarks
          path: ${{ inputs.benchmark-artifact-path }}
          if-no-files-found: ignore
      - name: Publish benchmark results
        if: ${{ inputs.benchmark-publish-branch != '' && (inputs.benchmark-publish-job == '' || inputs.benchmark-publish-job == 'std17-centos7-arm64') && success() && (github.event_name == 'push' || github.event_name == 'workflow_dispatch') }}
        shell: bash
        env:
          PUBLISH_BRANCH: ${{ inputs.benchmark-publish-branch }}
          SRC_DIR: ${{ inputs.benchmark-artifact-path }}
          GITHUB_TOKEN: ${{ github.token }}
          GITHUB_REPOSITORY: ${{ github.repository }}
        run: |
          set -euo pipefail
          if [ ! -d "$SRC_DIR" ]; then
            echo "No benchmark results at $SRC_DIR; skip publish"
            exit 0
          fi
          JOB_ID="${GITHUB_JOB}"
          ROOT="$GITHUB_WORKSPACE"
          cd "$ROOT"
          git config --global --add safe.directory "$ROOT"
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git remote set-url origin "https://x-access-token:${GITHUB_TOKEN}@github.com/${GITHUB_REPOSITORY}.git"
          WORK="$RUNNER_TEMP/bench-publish-$JOB_ID"
          cleanup_worktree() {
            cd "$ROOT"
            git worktree remove --force "$WORK" 2>/dev/null || true
            rm -rf "$WORK"
          }
          attempt=1
          while [ "$attempt" -le 12 ]; do
            echo "Publish attempt $attempt for $JOB_ID"
            cleanup_worktree
            mkdir -p "$WORK"
            if git fetch --depth 1 origin "$PUBLISH_BRANCH"; then
              git worktree add --force "$WORK" "FETCH_HEAD"
            else
              git worktree add --force --detach "$WORK"
              git -C "$WORK" checkout --orphan "$PUBLISH_BRANCH"
              git -C "$WORK" rm -rf . >/dev/null 2>&1 || true
            fi
            mkdir -p "$WORK/latest"
            rm -rf "$WORK/latest/$JOB_ID"
            mkdir -p "$WORK/latest/$JOB_ID"
            cp -a "$SRC_DIR"/. "$WORK/latest/$JOB_ID/"
            git -C "$WORK" add -A
            if git -C "$WORK" diff --cached --quiet; then
              echo "No benchmark publish changes for $JOB_ID"
              cleanup_worktree
              exit 0
            fi
            git -C "$WORK" commit -m "chore: update benchmark results for ${JOB_ID}"
            if git -C "$WORK" push origin "HEAD:$PUBLISH_BRANCH"; then
              echo "Published $JOB_ID to $PUBLISH_BRANCH/latest/$JOB_ID"
              cleanup_worktree
              exit 0
            fi
            echo "Push conflict; retrying..."
            cleanup_worktree
            attempt=$((attempt + 1))
            sleep $((attempt * 2))
          done
          echo "Failed to publish after retries"
          exit 1
      - name: Gather logs
        if: failure()
        shell: bash
        run: |
          SCRIPT="$(mktemp)"
          echo '' | base64 -d > "$SCRIPT"
          docker run --rm \
            -v "$SCRIPT:/tmp/xci-step.sh:ro" \
            -v "$GITHUB_WORKSPACE:$GITHUB_WORKSPACE" \
            -v "$RUNNER_TEMP:$RUNNER_TEMP" \
            -v "$GITHUB_ENV:$GITHUB_ENV" \
            -v "$GITHUB_PATH:$GITHUB_PATH" \
            -e GITHUB_ENV -e GITHUB_PATH -e RUNNER_TEMP -e GITHUB_WORKSPACE -e GITHUB_JOB \
            -e VCPKG_ROOT -e VCPKG_CMAKE -e VCPKG_DEFAULT_BINARY_CACHE -e VCPKG_FORCE_SYSTEM_BINARIES \
            -e KMPKG_HOME -e KMPKG_CMAKE -e KMPKG_FORCE_SYSTEM_BINARIES \
            -w "$GITHUB_WORKSPACE" \
            ghcr.io/koomx/kumo-std17-centos7-arm64:latest \
            sh /tmp/xci-step.sh
      - name: Upload logs on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: std17-centos7-arm64-logs
          path: |
            build/CMakeFiles/CMakeOutput.log
            build/CMakeFiles/CMakeError.log
            build/Testing/Temporary/
            build/vcpkg-logs/
          if-no-files-found: ignore

  std20-ubuntu22-amd64:
    if: ${{ inputs.enable-std20-ubuntu22-amd64 }}
    runs-on: ubuntu-latest
    container: ghcr.io/koomx/kumo-std20-ubuntu22-amd64:latest
    steps:
      - uses: actions/checkout@v4
      - name: Dump environment
        run: |
          echo "=== Environment Dump ==="
          if [ -f /etc/os-release ]; then
              . /etc/os-release
              echo "OS: $NAME $VERSION ($(uname -m))"
          elif [ "$(uname)" = "Darwin" ]; then
              echo "OS: macOS $(sw_vers -productVersion 2>/dev/null || echo unknown) ($(uname -m))"
          else
              echo "OS: $(uname -a 2>/dev/null || echo unknown)"
          fi
          for tool in cmake ninja make git python3 curl tar gzip; do
              if command -v $tool &>/dev/null; then
                  echo "  $tool: $(command -v $tool)  ($($tool --version 2>&1 | head -1))"
              else
                  echo "  $tool: NOT FOUND"
              fi
          done
          for cc in gcc g++ clang clang++ cc c++; do
              if command -v $cc &>/dev/null; then
                  echo "  $($cc --version 2>&1 | head -1)"
                  break
              fi
          done
          echo "=== End ==="
      - name: Install deps
        run: |
          if [ -n "${{ inputs.deps-ubuntu }}" ]; then
            apt-get update -qq
            apt-get install -y -qq ${{ inputs.deps-ubuntu }}
          fi
      - name: Bootstrap vcpkg
        run: |
          git clone --depth 1 --branch master https://github.com/microsoft/vcpkg.git /tmp/vcpkg
          /tmp/vcpkg/bootstrap-vcpkg.sh -disableMetrics
          echo "VCPKG_ROOT=/tmp/vcpkg" >> "$GITHUB_ENV"
          echo "VCPKG_CMAKE=/tmp/vcpkg/scripts/buildsystems/vcpkg.cmake" >> "$GITHUB_ENV"
          echo "VCPKG_FORCE_SYSTEM_BINARIES=1" >> "$GITHUB_ENV"
          mkdir -p "$RUNNER_TEMP/vcpkg-binary-cache"
          echo "VCPKG_DEFAULT_BINARY_CACHE=$RUNNER_TEMP/vcpkg-binary-cache" >> "$GITHUB_ENV"
      - name: Cache vcpkg binaries
        uses: actions/cache@v4
        with:
          path: ${{ runner.temp }}/vcpkg-binary-cache
          key: vcpkg-bin-std20-ubuntu22-amd64-${{ hashFiles('vcpkg.json', 'vcpkg-configuration.json', 'vcpkg-lock.json') }}
          restore-keys: |
            vcpkg-bin-std20-ubuntu22-amd64-
      - name: Configure
        run: ${{ inputs.config-command }}
      - name: Build
        run: ${{ inputs.build-command }}
      - name: Test
        if: ${{ inputs.test-command != '' }}
        run: ${{ inputs.test-command }}
      - name: Benchmark
        if: ${{ inputs.benchmark-command != '' }}
        run: ${{ inputs.benchmark-command }}
      - name: Plot
        if: ${{ inputs.plot-command != '' }}
        run: ${{ inputs.plot-command }}
      - name: Upload benchmark results
        if: ${{ (inputs.benchmark-command != '' || inputs.plot-command != '') && !cancelled() }}
        uses: actions/upload-artifact@v4
        with:
          name: std20-ubuntu22-amd64-benchmarks
          path: ${{ inputs.benchmark-artifact-path }}
          if-no-files-found: ignore
      - name: Publish benchmark results
        if: ${{ inputs.benchmark-publish-branch != '' && (inputs.benchmark-publish-job == '' || inputs.benchmark-publish-job == 'std20-ubuntu22-amd64') && success() && (github.event_name == 'push' || github.event_name == 'workflow_dispatch') }}
        shell: bash
        env:
          PUBLISH_BRANCH: ${{ inputs.benchmark-publish-branch }}
          SRC_DIR: ${{ inputs.benchmark-artifact-path }}
          GITHUB_TOKEN: ${{ github.token }}
          GITHUB_REPOSITORY: ${{ github.repository }}
        run: |
          set -euo pipefail
          if [ ! -d "$SRC_DIR" ]; then
            echo "No benchmark results at $SRC_DIR; skip publish"
            exit 0
          fi
          JOB_ID="${GITHUB_JOB}"
          ROOT="$GITHUB_WORKSPACE"
          cd "$ROOT"
          git config --global --add safe.directory "$ROOT"
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git remote set-url origin "https://x-access-token:${GITHUB_TOKEN}@github.com/${GITHUB_REPOSITORY}.git"
          WORK="$RUNNER_TEMP/bench-publish-$JOB_ID"
          cleanup_worktree() {
            cd "$ROOT"
            git worktree remove --force "$WORK" 2>/dev/null || true
            rm -rf "$WORK"
          }
          attempt=1
          while [ "$attempt" -le 12 ]; do
            echo "Publish attempt $attempt for $JOB_ID"
            cleanup_worktree
            mkdir -p "$WORK"
            if git fetch --depth 1 origin "$PUBLISH_BRANCH"; then
              git worktree add --force "$WORK" "FETCH_HEAD"
            else
              git worktree add --force --detach "$WORK"
              git -C "$WORK" checkout --orphan "$PUBLISH_BRANCH"
              git -C "$WORK" rm -rf . >/dev/null 2>&1 || true
            fi
            mkdir -p "$WORK/latest"
            rm -rf "$WORK/latest/$JOB_ID"
            mkdir -p "$WORK/latest/$JOB_ID"
            cp -a "$SRC_DIR"/. "$WORK/latest/$JOB_ID/"
            git -C "$WORK" add -A
            if git -C "$WORK" diff --cached --quiet; then
              echo "No benchmark publish changes for $JOB_ID"
              cleanup_worktree
              exit 0
            fi
            git -C "$WORK" commit -m "chore: update benchmark results for ${JOB_ID}"
            if git -C "$WORK" push origin "HEAD:$PUBLISH_BRANCH"; then
              echo "Published $JOB_ID to $PUBLISH_BRANCH/latest/$JOB_ID"
              cleanup_worktree
              exit 0
            fi
            echo "Push conflict; retrying..."
            cleanup_worktree
            attempt=$((attempt + 1))
            sleep $((attempt * 2))
          done
          echo "Failed to publish after retries"
          exit 1
      - name: Gather vcpkg logs
        if: failure()
        run: |
          mkdir -p build/vcpkg-logs
          if [ -d /tmp/vcpkg/buildtrees ]; then
            find /tmp/vcpkg/buildtrees -name "*.log" | while IFS= read -r f; do
              rel="${f#/tmp/vcpkg/}"
              mkdir -p "build/vcpkg-logs/$(dirname "$rel")"
              cp "$f" "build/vcpkg-logs/$rel"
            done
          fi
      - name: Upload logs on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: std20-ubuntu22-amd64-logs
          path: |
            build/CMakeFiles/CMakeOutput.log
            build/CMakeFiles/CMakeError.log
            build/Testing/Temporary/
            build/vcpkg-logs/
          if-no-files-found: ignore


  std20-ubuntu22-arm64:
    if: ${{ inputs.enable-std20-ubuntu22-arm64 }}
    runs-on: ubuntu-24.04-arm
    container: ghcr.io/koomx/kumo-std20-ubuntu22-arm64:latest
    steps:
      - uses: actions/checkout@v4
      - name: Dump environment
        run: |
          echo "=== Environment Dump ==="
          if [ -f /etc/os-release ]; then
              . /etc/os-release
              echo "OS: $NAME $VERSION ($(uname -m))"
          elif [ "$(uname)" = "Darwin" ]; then
              echo "OS: macOS $(sw_vers -productVersion 2>/dev/null || echo unknown) ($(uname -m))"
          else
              echo "OS: $(uname -a 2>/dev/null || echo unknown)"
          fi
          for tool in cmake ninja make git python3 curl tar gzip; do
              if command -v $tool &>/dev/null; then
                  echo "  $tool: $(command -v $tool)  ($($tool --version 2>&1 | head -1))"
              else
                  echo "  $tool: NOT FOUND"
              fi
          done
          for cc in gcc g++ clang clang++ cc c++; do
              if command -v $cc &>/dev/null; then
                  echo "  $($cc --version 2>&1 | head -1)"
                  break
              fi
          done
          echo "=== End ==="
      - name: Install deps
        run: |
          if [ -n "${{ inputs.deps-ubuntu }}" ]; then
            apt-get update -qq
            apt-get install -y -qq ${{ inputs.deps-ubuntu }}
          fi
      - name: Bootstrap vcpkg
        run: |
          git clone --depth 1 --branch master https://github.com/microsoft/vcpkg.git /tmp/vcpkg
          /tmp/vcpkg/bootstrap-vcpkg.sh -disableMetrics
          echo "VCPKG_ROOT=/tmp/vcpkg" >> "$GITHUB_ENV"
          echo "VCPKG_CMAKE=/tmp/vcpkg/scripts/buildsystems/vcpkg.cmake" >> "$GITHUB_ENV"
          echo "VCPKG_FORCE_SYSTEM_BINARIES=1" >> "$GITHUB_ENV"
          mkdir -p "$RUNNER_TEMP/vcpkg-binary-cache"
          echo "VCPKG_DEFAULT_BINARY_CACHE=$RUNNER_TEMP/vcpkg-binary-cache" >> "$GITHUB_ENV"
      - name: Cache vcpkg binaries
        uses: actions/cache@v4
        with:
          path: ${{ runner.temp }}/vcpkg-binary-cache
          key: vcpkg-bin-std20-ubuntu22-arm64-${{ hashFiles('vcpkg.json', 'vcpkg-configuration.json', 'vcpkg-lock.json') }}
          restore-keys: |
            vcpkg-bin-std20-ubuntu22-arm64-
      - name: Configure
        run: ${{ inputs.config-command }}
      - name: Build
        run: ${{ inputs.build-command }}
      - name: Test
        if: ${{ inputs.test-command != '' }}
        run: ${{ inputs.test-command }}
      - name: Benchmark
        if: ${{ inputs.benchmark-command != '' }}
        run: ${{ inputs.benchmark-command }}
      - name: Plot
        if: ${{ inputs.plot-command != '' }}
        run: ${{ inputs.plot-command }}
      - name: Upload benchmark results
        if: ${{ (inputs.benchmark-command != '' || inputs.plot-command != '') && !cancelled() }}
        uses: actions/upload-artifact@v4
        with:
          name: std20-ubuntu22-arm64-benchmarks
          path: ${{ inputs.benchmark-artifact-path }}
          if-no-files-found: ignore
      - name: Publish benchmark results
        if: ${{ inputs.benchmark-publish-branch != '' && (inputs.benchmark-publish-job == '' || inputs.benchmark-publish-job == 'std20-ubuntu22-arm64') && success() && (github.event_name == 'push' || github.event_name == 'workflow_dispatch') }}
        shell: bash
        env:
          PUBLISH_BRANCH: ${{ inputs.benchmark-publish-branch }}
          SRC_DIR: ${{ inputs.benchmark-artifact-path }}
          GITHUB_TOKEN: ${{ github.token }}
          GITHUB_REPOSITORY: ${{ github.repository }}
        run: |
          set -euo pipefail
          if [ ! -d "$SRC_DIR" ]; then
            echo "No benchmark results at $SRC_DIR; skip publish"
            exit 0
          fi
          JOB_ID="${GITHUB_JOB}"
          ROOT="$GITHUB_WORKSPACE"
          cd "$ROOT"
          git config --global --add safe.directory "$ROOT"
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git remote set-url origin "https://x-access-token:${GITHUB_TOKEN}@github.com/${GITHUB_REPOSITORY}.git"
          WORK="$RUNNER_TEMP/bench-publish-$JOB_ID"
          cleanup_worktree() {
            cd "$ROOT"
            git worktree remove --force "$WORK" 2>/dev/null || true
            rm -rf "$WORK"
          }
          attempt=1
          while [ "$attempt" -le 12 ]; do
            echo "Publish attempt $attempt for $JOB_ID"
            cleanup_worktree
            mkdir -p "$WORK"
            if git fetch --depth 1 origin "$PUBLISH_BRANCH"; then
              git worktree add --force "$WORK" "FETCH_HEAD"
            else
              git worktree add --force --detach "$WORK"
              git -C "$WORK" checkout --orphan "$PUBLISH_BRANCH"
              git -C "$WORK" rm -rf . >/dev/null 2>&1 || true
            fi
            mkdir -p "$WORK/latest"
            rm -rf "$WORK/latest/$JOB_ID"
            mkdir -p "$WORK/latest/$JOB_ID"
            cp -a "$SRC_DIR"/. "$WORK/latest/$JOB_ID/"
            git -C "$WORK" add -A
            if git -C "$WORK" diff --cached --quiet; then
              echo "No benchmark publish changes for $JOB_ID"
              cleanup_worktree
              exit 0
            fi
            git -C "$WORK" commit -m "chore: update benchmark results for ${JOB_ID}"
            if git -C "$WORK" push origin "HEAD:$PUBLISH_BRANCH"; then
              echo "Published $JOB_ID to $PUBLISH_BRANCH/latest/$JOB_ID"
              cleanup_worktree
              exit 0
            fi
            echo "Push conflict; retrying..."
            cleanup_worktree
            attempt=$((attempt + 1))
            sleep $((attempt * 2))
          done
          echo "Failed to publish after retries"
          exit 1
      - name: Gather vcpkg logs
        if: failure()
        run: |
          mkdir -p build/vcpkg-logs
          if [ -d /tmp/vcpkg/buildtrees ]; then
            find /tmp/vcpkg/buildtrees -name "*.log" | while IFS= read -r f; do
              rel="${f#/tmp/vcpkg/}"
              mkdir -p "build/vcpkg-logs/$(dirname "$rel")"
              cp "$f" "build/vcpkg-logs/$rel"
            done
          fi
      - name: Upload logs on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: std20-ubuntu22-arm64-logs
          path: |
            build/CMakeFiles/CMakeOutput.log
            build/CMakeFiles/CMakeError.log
            build/Testing/Temporary/
            build/vcpkg-logs/
          if-no-files-found: ignore


  std20-ubuntu24-amd64:
    if: ${{ inputs.enable-std20-ubuntu24-amd64 }}
    runs-on: ubuntu-latest
    container: ghcr.io/koomx/kumo-std20-ubuntu24-amd64:latest
    steps:
      - uses: actions/checkout@v4
      - name: Dump environment
        run: |
          echo "=== Environment Dump ==="
          if [ -f /etc/os-release ]; then
              . /etc/os-release
              echo "OS: $NAME $VERSION ($(uname -m))"
          elif [ "$(uname)" = "Darwin" ]; then
              echo "OS: macOS $(sw_vers -productVersion 2>/dev/null || echo unknown) ($(uname -m))"
          else
              echo "OS: $(uname -a 2>/dev/null || echo unknown)"
          fi
          for tool in cmake ninja make git python3 curl tar gzip; do
              if command -v $tool &>/dev/null; then
                  echo "  $tool: $(command -v $tool)  ($($tool --version 2>&1 | head -1))"
              else
                  echo "  $tool: NOT FOUND"
              fi
          done
          for cc in gcc g++ clang clang++ cc c++; do
              if command -v $cc &>/dev/null; then
                  echo "  $($cc --version 2>&1 | head -1)"
                  break
              fi
          done
          echo "=== End ==="
      - name: Install deps
        run: |
          if [ -n "${{ inputs.deps-ubuntu }}" ]; then
            apt-get update -qq
            apt-get install -y -qq ${{ inputs.deps-ubuntu }}
          fi
      - name: Bootstrap vcpkg
        run: |
          git clone --depth 1 --branch master https://github.com/microsoft/vcpkg.git /tmp/vcpkg
          /tmp/vcpkg/bootstrap-vcpkg.sh -disableMetrics
          echo "VCPKG_ROOT=/tmp/vcpkg" >> "$GITHUB_ENV"
          echo "VCPKG_CMAKE=/tmp/vcpkg/scripts/buildsystems/vcpkg.cmake" >> "$GITHUB_ENV"
          echo "VCPKG_FORCE_SYSTEM_BINARIES=1" >> "$GITHUB_ENV"
          mkdir -p "$RUNNER_TEMP/vcpkg-binary-cache"
          echo "VCPKG_DEFAULT_BINARY_CACHE=$RUNNER_TEMP/vcpkg-binary-cache" >> "$GITHUB_ENV"
      - name: Cache vcpkg binaries
        uses: actions/cache@v4
        with:
          path: ${{ runner.temp }}/vcpkg-binary-cache
          key: vcpkg-bin-std20-ubuntu24-amd64-${{ hashFiles('vcpkg.json', 'vcpkg-configuration.json', 'vcpkg-lock.json') }}
          restore-keys: |
            vcpkg-bin-std20-ubuntu24-amd64-
      - name: Configure
        run: ${{ inputs.config-command }}
      - name: Build
        run: ${{ inputs.build-command }}
      - name: Test
        if: ${{ inputs.test-command != '' }}
        run: ${{ inputs.test-command }}
      - name: Benchmark
        if: ${{ inputs.benchmark-command != '' }}
        run: ${{ inputs.benchmark-command }}
      - name: Plot
        if: ${{ inputs.plot-command != '' }}
        run: ${{ inputs.plot-command }}
      - name: Upload benchmark results
        if: ${{ (inputs.benchmark-command != '' || inputs.plot-command != '') && !cancelled() }}
        uses: actions/upload-artifact@v4
        with:
          name: std20-ubuntu24-amd64-benchmarks
          path: ${{ inputs.benchmark-artifact-path }}
          if-no-files-found: ignore
      - name: Publish benchmark results
        if: ${{ inputs.benchmark-publish-branch != '' && (inputs.benchmark-publish-job == '' || inputs.benchmark-publish-job == 'std20-ubuntu24-amd64') && success() && (github.event_name == 'push' || github.event_name == 'workflow_dispatch') }}
        shell: bash
        env:
          PUBLISH_BRANCH: ${{ inputs.benchmark-publish-branch }}
          SRC_DIR: ${{ inputs.benchmark-artifact-path }}
          GITHUB_TOKEN: ${{ github.token }}
          GITHUB_REPOSITORY: ${{ github.repository }}
        run: |
          set -euo pipefail
          if [ ! -d "$SRC_DIR" ]; then
            echo "No benchmark results at $SRC_DIR; skip publish"
            exit 0
          fi
          JOB_ID="${GITHUB_JOB}"
          ROOT="$GITHUB_WORKSPACE"
          cd "$ROOT"
          git config --global --add safe.directory "$ROOT"
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git remote set-url origin "https://x-access-token:${GITHUB_TOKEN}@github.com/${GITHUB_REPOSITORY}.git"
          WORK="$RUNNER_TEMP/bench-publish-$JOB_ID"
          cleanup_worktree() {
            cd "$ROOT"
            git worktree remove --force "$WORK" 2>/dev/null || true
            rm -rf "$WORK"
          }
          attempt=1
          while [ "$attempt" -le 12 ]; do
            echo "Publish attempt $attempt for $JOB_ID"
            cleanup_worktree
            mkdir -p "$WORK"
            if git fetch --depth 1 origin "$PUBLISH_BRANCH"; then
              git worktree add --force "$WORK" "FETCH_HEAD"
            else
              git worktree add --force --detach "$WORK"
              git -C "$WORK" checkout --orphan "$PUBLISH_BRANCH"
              git -C "$WORK" rm -rf . >/dev/null 2>&1 || true
            fi
            mkdir -p "$WORK/latest"
            rm -rf "$WORK/latest/$JOB_ID"
            mkdir -p "$WORK/latest/$JOB_ID"
            cp -a "$SRC_DIR"/. "$WORK/latest/$JOB_ID/"
            git -C "$WORK" add -A
            if git -C "$WORK" diff --cached --quiet; then
              echo "No benchmark publish changes for $JOB_ID"
              cleanup_worktree
              exit 0
            fi
            git -C "$WORK" commit -m "chore: update benchmark results for ${JOB_ID}"
            if git -C "$WORK" push origin "HEAD:$PUBLISH_BRANCH"; then
              echo "Published $JOB_ID to $PUBLISH_BRANCH/latest/$JOB_ID"
              cleanup_worktree
              exit 0
            fi
            echo "Push conflict; retrying..."
            cleanup_worktree
            attempt=$((attempt + 1))
            sleep $((attempt * 2))
          done
          echo "Failed to publish after retries"
          exit 1
      - name: Gather vcpkg logs
        if: failure()
        run: |
          mkdir -p build/vcpkg-logs
          if [ -d /tmp/vcpkg/buildtrees ]; then
            find /tmp/vcpkg/buildtrees -name "*.log" | while IFS= read -r f; do
              rel="${f#/tmp/vcpkg/}"
              mkdir -p "build/vcpkg-logs/$(dirname "$rel")"
              cp "$f" "build/vcpkg-logs/$rel"
            done
          fi
      - name: Upload logs on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: std20-ubuntu24-amd64-logs
          path: |
            build/CMakeFiles/CMakeOutput.log
            build/CMakeFiles/CMakeError.log
            build/Testing/Temporary/
            build/vcpkg-logs/
          if-no-files-found: ignore


  std20-ubuntu24-arm64:
    if: ${{ inputs.enable-std20-ubuntu24-arm64 }}
    runs-on: ubuntu-24.04-arm
    container: ghcr.io/koomx/kumo-std20-ubuntu24-arm64:latest
    steps:
      - uses: actions/checkout@v4
      - name: Dump environment
        run: |
          echo "=== Environment Dump ==="
          if [ -f /etc/os-release ]; then
              . /etc/os-release
              echo "OS: $NAME $VERSION ($(uname -m))"
          elif [ "$(uname)" = "Darwin" ]; then
              echo "OS: macOS $(sw_vers -productVersion 2>/dev/null || echo unknown) ($(uname -m))"
          else
              echo "OS: $(uname -a 2>/dev/null || echo unknown)"
          fi
          for tool in cmake ninja make git python3 curl tar gzip; do
              if command -v $tool &>/dev/null; then
                  echo "  $tool: $(command -v $tool)  ($($tool --version 2>&1 | head -1))"
              else
                  echo "  $tool: NOT FOUND"
              fi
          done
          for cc in gcc g++ clang clang++ cc c++; do
              if command -v $cc &>/dev/null; then
                  echo "  $($cc --version 2>&1 | head -1)"
                  break
              fi
          done
          echo "=== End ==="
      - name: Install deps
        run: |
          if [ -n "${{ inputs.deps-ubuntu }}" ]; then
            apt-get update -qq
            apt-get install -y -qq ${{ inputs.deps-ubuntu }}
          fi
      - name: Bootstrap vcpkg
        run: |
          git clone --depth 1 --branch master https://github.com/microsoft/vcpkg.git /tmp/vcpkg
          /tmp/vcpkg/bootstrap-vcpkg.sh -disableMetrics
          echo "VCPKG_ROOT=/tmp/vcpkg" >> "$GITHUB_ENV"
          echo "VCPKG_CMAKE=/tmp/vcpkg/scripts/buildsystems/vcpkg.cmake" >> "$GITHUB_ENV"
          echo "VCPKG_FORCE_SYSTEM_BINARIES=1" >> "$GITHUB_ENV"
          mkdir -p "$RUNNER_TEMP/vcpkg-binary-cache"
          echo "VCPKG_DEFAULT_BINARY_CACHE=$RUNNER_TEMP/vcpkg-binary-cache" >> "$GITHUB_ENV"
      - name: Cache vcpkg binaries
        uses: actions/cache@v4
        with:
          path: ${{ runner.temp }}/vcpkg-binary-cache
          key: vcpkg-bin-std20-ubuntu24-arm64-${{ hashFiles('vcpkg.json', 'vcpkg-configuration.json', 'vcpkg-lock.json') }}
          restore-keys: |
            vcpkg-bin-std20-ubuntu24-arm64-
      - name: Configure
        run: ${{ inputs.config-command }}
      - name: Build
        run: ${{ inputs.build-command }}
      - name: Test
        if: ${{ inputs.test-command != '' }}
        run: ${{ inputs.test-command }}
      - name: Benchmark
        if: ${{ inputs.benchmark-command != '' }}
        run: ${{ inputs.benchmark-command }}
      - name: Plot
        if: ${{ inputs.plot-command != '' }}
        run: ${{ inputs.plot-command }}
      - name: Upload benchmark results
        if: ${{ (inputs.benchmark-command != '' || inputs.plot-command != '') && !cancelled() }}
        uses: actions/upload-artifact@v4
        with:
          name: std20-ubuntu24-arm64-benchmarks
          path: ${{ inputs.benchmark-artifact-path }}
          if-no-files-found: ignore
      - name: Publish benchmark results
        if: ${{ inputs.benchmark-publish-branch != '' && (inputs.benchmark-publish-job == '' || inputs.benchmark-publish-job == 'std20-ubuntu24-arm64') && success() && (github.event_name == 'push' || github.event_name == 'workflow_dispatch') }}
        shell: bash
        env:
          PUBLISH_BRANCH: ${{ inputs.benchmark-publish-branch }}
          SRC_DIR: ${{ inputs.benchmark-artifact-path }}
          GITHUB_TOKEN: ${{ github.token }}
          GITHUB_REPOSITORY: ${{ github.repository }}
        run: |
          set -euo pipefail
          if [ ! -d "$SRC_DIR" ]; then
            echo "No benchmark results at $SRC_DIR; skip publish"
            exit 0
          fi
          JOB_ID="${GITHUB_JOB}"
          ROOT="$GITHUB_WORKSPACE"
          cd "$ROOT"
          git config --global --add safe.directory "$ROOT"
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git remote set-url origin "https://x-access-token:${GITHUB_TOKEN}@github.com/${GITHUB_REPOSITORY}.git"
          WORK="$RUNNER_TEMP/bench-publish-$JOB_ID"
          cleanup_worktree() {
            cd "$ROOT"
            git worktree remove --force "$WORK" 2>/dev/null || true
            rm -rf "$WORK"
          }
          attempt=1
          while [ "$attempt" -le 12 ]; do
            echo "Publish attempt $attempt for $JOB_ID"
            cleanup_worktree
            mkdir -p "$WORK"
            if git fetch --depth 1 origin "$PUBLISH_BRANCH"; then
              git worktree add --force "$WORK" "FETCH_HEAD"
            else
              git worktree add --force --detach "$WORK"
              git -C "$WORK" checkout --orphan "$PUBLISH_BRANCH"
              git -C "$WORK" rm -rf . >/dev/null 2>&1 || true
            fi
            mkdir -p "$WORK/latest"
            rm -rf "$WORK/latest/$JOB_ID"
            mkdir -p "$WORK/latest/$JOB_ID"
            cp -a "$SRC_DIR"/. "$WORK/latest/$JOB_ID/"
            git -C "$WORK" add -A
            if git -C "$WORK" diff --cached --quiet; then
              echo "No benchmark publish changes for $JOB_ID"
              cleanup_worktree
              exit 0
            fi
            git -C "$WORK" commit -m "chore: update benchmark results for ${JOB_ID}"
            if git -C "$WORK" push origin "HEAD:$PUBLISH_BRANCH"; then
              echo "Published $JOB_ID to $PUBLISH_BRANCH/latest/$JOB_ID"
              cleanup_worktree
              exit 0
            fi
            echo "Push conflict; retrying..."
            cleanup_worktree
            attempt=$((attempt + 1))
            sleep $((attempt * 2))
          done
          echo "Failed to publish after retries"
          exit 1
      - name: Gather vcpkg logs
        if: failure()
        run: |
          mkdir -p build/vcpkg-logs
          if [ -d /tmp/vcpkg/buildtrees ]; then
            find /tmp/vcpkg/buildtrees -name "*.log" | while IFS= read -r f; do
              rel="${f#/tmp/vcpkg/}"
              mkdir -p "build/vcpkg-logs/$(dirname "$rel")"
              cp "$f" "build/vcpkg-logs/$rel"
            done
          fi
      - name: Upload logs on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: std20-ubuntu24-arm64-logs
          path: |
            build/CMakeFiles/CMakeOutput.log
            build/CMakeFiles/CMakeError.log
            build/Testing/Temporary/
            build/vcpkg-logs/
          if-no-files-found: ignore


  std20-debian12-amd64:
    if: ${{ inputs.enable-std20-debian12-amd64 }}
    runs-on: ubuntu-latest
    container: ghcr.io/koomx/kumo-std20-debian12-amd64:latest
    steps:
      - uses: actions/checkout@v4
      - name: Dump environment
        run: |
          echo "=== Environment Dump ==="
          if [ -f /etc/os-release ]; then
              . /etc/os-release
              echo "OS: $NAME $VERSION ($(uname -m))"
          elif [ "$(uname)" = "Darwin" ]; then
              echo "OS: macOS $(sw_vers -productVersion 2>/dev/null || echo unknown) ($(uname -m))"
          else
              echo "OS: $(uname -a 2>/dev/null || echo unknown)"
          fi
          for tool in cmake ninja make git python3 curl tar gzip; do
              if command -v $tool &>/dev/null; then
                  echo "  $tool: $(command -v $tool)  ($($tool --version 2>&1 | head -1))"
              else
                  echo "  $tool: NOT FOUND"
              fi
          done
          for cc in gcc g++ clang clang++ cc c++; do
              if command -v $cc &>/dev/null; then
                  echo "  $($cc --version 2>&1 | head -1)"
                  break
              fi
          done
          echo "=== End ==="
      - name: Install deps
        run: |
          if [ -n "${{ inputs.deps-ubuntu }}" ]; then
            apt-get update -qq
            apt-get install -y -qq ${{ inputs.deps-ubuntu }}
          fi
      - name: Bootstrap vcpkg
        run: |
          git clone --depth 1 --branch master https://github.com/microsoft/vcpkg.git /tmp/vcpkg
          /tmp/vcpkg/bootstrap-vcpkg.sh -disableMetrics
          echo "VCPKG_ROOT=/tmp/vcpkg" >> "$GITHUB_ENV"
          echo "VCPKG_CMAKE=/tmp/vcpkg/scripts/buildsystems/vcpkg.cmake" >> "$GITHUB_ENV"
          echo "VCPKG_FORCE_SYSTEM_BINARIES=1" >> "$GITHUB_ENV"
          mkdir -p "$RUNNER_TEMP/vcpkg-binary-cache"
          echo "VCPKG_DEFAULT_BINARY_CACHE=$RUNNER_TEMP/vcpkg-binary-cache" >> "$GITHUB_ENV"
      - name: Cache vcpkg binaries
        uses: actions/cache@v4
        with:
          path: ${{ runner.temp }}/vcpkg-binary-cache
          key: vcpkg-bin-std20-debian12-amd64-${{ hashFiles('vcpkg.json', 'vcpkg-configuration.json', 'vcpkg-lock.json') }}
          restore-keys: |
            vcpkg-bin-std20-debian12-amd64-
      - name: Configure
        run: ${{ inputs.config-command }}
      - name: Build
        run: ${{ inputs.build-command }}
      - name: Test
        if: ${{ inputs.test-command != '' }}
        run: ${{ inputs.test-command }}
      - name: Benchmark
        if: ${{ inputs.benchmark-command != '' }}
        run: ${{ inputs.benchmark-command }}
      - name: Plot
        if: ${{ inputs.plot-command != '' }}
        run: ${{ inputs.plot-command }}
      - name: Upload benchmark results
        if: ${{ (inputs.benchmark-command != '' || inputs.plot-command != '') && !cancelled() }}
        uses: actions/upload-artifact@v4
        with:
          name: std20-debian12-amd64-benchmarks
          path: ${{ inputs.benchmark-artifact-path }}
          if-no-files-found: ignore
      - name: Publish benchmark results
        if: ${{ inputs.benchmark-publish-branch != '' && (inputs.benchmark-publish-job == '' || inputs.benchmark-publish-job == 'std20-debian12-amd64') && success() && (github.event_name == 'push' || github.event_name == 'workflow_dispatch') }}
        shell: bash
        env:
          PUBLISH_BRANCH: ${{ inputs.benchmark-publish-branch }}
          SRC_DIR: ${{ inputs.benchmark-artifact-path }}
          GITHUB_TOKEN: ${{ github.token }}
          GITHUB_REPOSITORY: ${{ github.repository }}
        run: |
          set -euo pipefail
          if [ ! -d "$SRC_DIR" ]; then
            echo "No benchmark results at $SRC_DIR; skip publish"
            exit 0
          fi
          JOB_ID="${GITHUB_JOB}"
          ROOT="$GITHUB_WORKSPACE"
          cd "$ROOT"
          git config --global --add safe.directory "$ROOT"
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git remote set-url origin "https://x-access-token:${GITHUB_TOKEN}@github.com/${GITHUB_REPOSITORY}.git"
          WORK="$RUNNER_TEMP/bench-publish-$JOB_ID"
          cleanup_worktree() {
            cd "$ROOT"
            git worktree remove --force "$WORK" 2>/dev/null || true
            rm -rf "$WORK"
          }
          attempt=1
          while [ "$attempt" -le 12 ]; do
            echo "Publish attempt $attempt for $JOB_ID"
            cleanup_worktree
            mkdir -p "$WORK"
            if git fetch --depth 1 origin "$PUBLISH_BRANCH"; then
              git worktree add --force "$WORK" "FETCH_HEAD"
            else
              git worktree add --force --detach "$WORK"
              git -C "$WORK" checkout --orphan "$PUBLISH_BRANCH"
              git -C "$WORK" rm -rf . >/dev/null 2>&1 || true
            fi
            mkdir -p "$WORK/latest"
            rm -rf "$WORK/latest/$JOB_ID"
            mkdir -p "$WORK/latest/$JOB_ID"
            cp -a "$SRC_DIR"/. "$WORK/latest/$JOB_ID/"
            git -C "$WORK" add -A
            if git -C "$WORK" diff --cached --quiet; then
              echo "No benchmark publish changes for $JOB_ID"
              cleanup_worktree
              exit 0
            fi
            git -C "$WORK" commit -m "chore: update benchmark results for ${JOB_ID}"
            if git -C "$WORK" push origin "HEAD:$PUBLISH_BRANCH"; then
              echo "Published $JOB_ID to $PUBLISH_BRANCH/latest/$JOB_ID"
              cleanup_worktree
              exit 0
            fi
            echo "Push conflict; retrying..."
            cleanup_worktree
            attempt=$((attempt + 1))
            sleep $((attempt * 2))
          done
          echo "Failed to publish after retries"
          exit 1
      - name: Gather vcpkg logs
        if: failure()
        run: |
          mkdir -p build/vcpkg-logs
          if [ -d /tmp/vcpkg/buildtrees ]; then
            find /tmp/vcpkg/buildtrees -name "*.log" | while IFS= read -r f; do
              rel="${f#/tmp/vcpkg/}"
              mkdir -p "build/vcpkg-logs/$(dirname "$rel")"
              cp "$f" "build/vcpkg-logs/$rel"
            done
          fi
      - name: Upload logs on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: std20-debian12-amd64-logs
          path: |
            build/CMakeFiles/CMakeOutput.log
            build/CMakeFiles/CMakeError.log
            build/Testing/Temporary/
            build/vcpkg-logs/
          if-no-files-found: ignore


  std20-debian12-arm64:
    if: ${{ inputs.enable-std20-debian12-arm64 }}
    runs-on: ubuntu-24.04-arm
    container: ghcr.io/koomx/kumo-std20-debian12-arm64:latest
    steps:
      - uses: actions/checkout@v4
      - name: Dump environment
        run: |
          echo "=== Environment Dump ==="
          if [ -f /etc/os-release ]; then
              . /etc/os-release
              echo "OS: $NAME $VERSION ($(uname -m))"
          elif [ "$(uname)" = "Darwin" ]; then
              echo "OS: macOS $(sw_vers -productVersion 2>/dev/null || echo unknown) ($(uname -m))"
          else
              echo "OS: $(uname -a 2>/dev/null || echo unknown)"
          fi
          for tool in cmake ninja make git python3 curl tar gzip; do
              if command -v $tool &>/dev/null; then
                  echo "  $tool: $(command -v $tool)  ($($tool --version 2>&1 | head -1))"
              else
                  echo "  $tool: NOT FOUND"
              fi
          done
          for cc in gcc g++ clang clang++ cc c++; do
              if command -v $cc &>/dev/null; then
                  echo "  $($cc --version 2>&1 | head -1)"
                  break
              fi
          done
          echo "=== End ==="
      - name: Install deps
        run: |
          if [ -n "${{ inputs.deps-ubuntu }}" ]; then
            apt-get update -qq
            apt-get install -y -qq ${{ inputs.deps-ubuntu }}
          fi
      - name: Bootstrap vcpkg
        run: |
          git clone --depth 1 --branch master https://github.com/microsoft/vcpkg.git /tmp/vcpkg
          /tmp/vcpkg/bootstrap-vcpkg.sh -disableMetrics
          echo "VCPKG_ROOT=/tmp/vcpkg" >> "$GITHUB_ENV"
          echo "VCPKG_CMAKE=/tmp/vcpkg/scripts/buildsystems/vcpkg.cmake" >> "$GITHUB_ENV"
          echo "VCPKG_FORCE_SYSTEM_BINARIES=1" >> "$GITHUB_ENV"
          mkdir -p "$RUNNER_TEMP/vcpkg-binary-cache"
          echo "VCPKG_DEFAULT_BINARY_CACHE=$RUNNER_TEMP/vcpkg-binary-cache" >> "$GITHUB_ENV"
      - name: Cache vcpkg binaries
        uses: actions/cache@v4
        with:
          path: ${{ runner.temp }}/vcpkg-binary-cache
          key: vcpkg-bin-std20-debian12-arm64-${{ hashFiles('vcpkg.json', 'vcpkg-configuration.json', 'vcpkg-lock.json') }}
          restore-keys: |
            vcpkg-bin-std20-debian12-arm64-
      - name: Configure
        run: ${{ inputs.config-command }}
      - name: Build
        run: ${{ inputs.build-command }}
      - name: Test
        if: ${{ inputs.test-command != '' }}
        run: ${{ inputs.test-command }}
      - name: Benchmark
        if: ${{ inputs.benchmark-command != '' }}
        run: ${{ inputs.benchmark-command }}
      - name: Plot
        if: ${{ inputs.plot-command != '' }}
        run: ${{ inputs.plot-command }}
      - name: Upload benchmark results
        if: ${{ (inputs.benchmark-command != '' || inputs.plot-command != '') && !cancelled() }}
        uses: actions/upload-artifact@v4
        with:
          name: std20-debian12-arm64-benchmarks
          path: ${{ inputs.benchmark-artifact-path }}
          if-no-files-found: ignore
      - name: Publish benchmark results
        if: ${{ inputs.benchmark-publish-branch != '' && (inputs.benchmark-publish-job == '' || inputs.benchmark-publish-job == 'std20-debian12-arm64') && success() && (github.event_name == 'push' || github.event_name == 'workflow_dispatch') }}
        shell: bash
        env:
          PUBLISH_BRANCH: ${{ inputs.benchmark-publish-branch }}
          SRC_DIR: ${{ inputs.benchmark-artifact-path }}
          GITHUB_TOKEN: ${{ github.token }}
          GITHUB_REPOSITORY: ${{ github.repository }}
        run: |
          set -euo pipefail
          if [ ! -d "$SRC_DIR" ]; then
            echo "No benchmark results at $SRC_DIR; skip publish"
            exit 0
          fi
          JOB_ID="${GITHUB_JOB}"
          ROOT="$GITHUB_WORKSPACE"
          cd "$ROOT"
          git config --global --add safe.directory "$ROOT"
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git remote set-url origin "https://x-access-token:${GITHUB_TOKEN}@github.com/${GITHUB_REPOSITORY}.git"
          WORK="$RUNNER_TEMP/bench-publish-$JOB_ID"
          cleanup_worktree() {
            cd "$ROOT"
            git worktree remove --force "$WORK" 2>/dev/null || true
            rm -rf "$WORK"
          }
          attempt=1
          while [ "$attempt" -le 12 ]; do
            echo "Publish attempt $attempt for $JOB_ID"
            cleanup_worktree
            mkdir -p "$WORK"
            if git fetch --depth 1 origin "$PUBLISH_BRANCH"; then
              git worktree add --force "$WORK" "FETCH_HEAD"
            else
              git worktree add --force --detach "$WORK"
              git -C "$WORK" checkout --orphan "$PUBLISH_BRANCH"
              git -C "$WORK" rm -rf . >/dev/null 2>&1 || true
            fi
            mkdir -p "$WORK/latest"
            rm -rf "$WORK/latest/$JOB_ID"
            mkdir -p "$WORK/latest/$JOB_ID"
            cp -a "$SRC_DIR"/. "$WORK/latest/$JOB_ID/"
            git -C "$WORK" add -A
            if git -C "$WORK" diff --cached --quiet; then
              echo "No benchmark publish changes for $JOB_ID"
              cleanup_worktree
              exit 0
            fi
            git -C "$WORK" commit -m "chore: update benchmark results for ${JOB_ID}"
            if git -C "$WORK" push origin "HEAD:$PUBLISH_BRANCH"; then
              echo "Published $JOB_ID to $PUBLISH_BRANCH/latest/$JOB_ID"
              cleanup_worktree
              exit 0
            fi
            echo "Push conflict; retrying..."
            cleanup_worktree
            attempt=$((attempt + 1))
            sleep $((attempt * 2))
          done
          echo "Failed to publish after retries"
          exit 1
      - name: Gather vcpkg logs
        if: failure()
        run: |
          mkdir -p build/vcpkg-logs
          if [ -d /tmp/vcpkg/buildtrees ]; then
            find /tmp/vcpkg/buildtrees -name "*.log" | while IFS= read -r f; do
              rel="${f#/tmp/vcpkg/}"
              mkdir -p "build/vcpkg-logs/$(dirname "$rel")"
              cp "$f" "build/vcpkg-logs/$rel"
            done
          fi
      - name: Upload logs on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: std20-debian12-arm64-logs
          path: |
            build/CMakeFiles/CMakeOutput.log
            build/CMakeFiles/CMakeError.log
            build/Testing/Temporary/
            build/vcpkg-logs/
          if-no-files-found: ignore


  std20-alpine319-amd64:
    if: ${{ inputs.enable-std20-alpine319-amd64 }}
    runs-on: ubuntu-latest
    container: ghcr.io/koomx/kumo-std20-alpine319-amd64:latest
    steps:
      - uses: actions/checkout@v4
      - name: Dump environment
        run: |
          echo "=== Environment Dump ==="
          if [ -f /etc/os-release ]; then
              . /etc/os-release
              echo "OS: $NAME $VERSION ($(uname -m))"
          elif [ "$(uname)" = "Darwin" ]; then
              echo "OS: macOS $(sw_vers -productVersion 2>/dev/null || echo unknown) ($(uname -m))"
          else
              echo "OS: $(uname -a 2>/dev/null || echo unknown)"
          fi
          for tool in cmake ninja make git python3 curl tar gzip; do
              if command -v $tool &>/dev/null; then
                  echo "  $tool: $(command -v $tool)  ($($tool --version 2>&1 | head -1))"
              else
                  echo "  $tool: NOT FOUND"
              fi
          done
          for cc in gcc g++ clang clang++ cc c++; do
              if command -v $cc &>/dev/null; then
                  echo "  $($cc --version 2>&1 | head -1)"
                  break
              fi
          done
          echo "=== End ==="
      - name: Install deps
        run: |
          if [ -n "${{ inputs.deps-alpine }}" ]; then
            apk add --no-cache ${{ inputs.deps-alpine }}
          fi
      - name: Bootstrap vcpkg
        run: |
          git clone --depth 1 --branch master https://github.com/microsoft/vcpkg.git /tmp/vcpkg
          /tmp/vcpkg/bootstrap-vcpkg.sh -disableMetrics
          echo "VCPKG_ROOT=/tmp/vcpkg" >> "$GITHUB_ENV"
          echo "VCPKG_CMAKE=/tmp/vcpkg/scripts/buildsystems/vcpkg.cmake" >> "$GITHUB_ENV"
          echo "VCPKG_FORCE_SYSTEM_BINARIES=1" >> "$GITHUB_ENV"
          mkdir -p "$RUNNER_TEMP/vcpkg-binary-cache"
          echo "VCPKG_DEFAULT_BINARY_CACHE=$RUNNER_TEMP/vcpkg-binary-cache" >> "$GITHUB_ENV"
      - name: Cache vcpkg binaries
        uses: actions/cache@v4
        with:
          path: ${{ runner.temp }}/vcpkg-binary-cache
          key: vcpkg-bin-std20-alpine319-amd64-${{ hashFiles('vcpkg.json', 'vcpkg-configuration.json', 'vcpkg-lock.json') }}
          restore-keys: |
            vcpkg-bin-std20-alpine319-amd64-
      - name: Configure
        run: ${{ inputs.config-command }}
      - name: Build
        run: ${{ inputs.build-command }}
      - name: Test
        if: ${{ inputs.test-command != '' }}
        run: ${{ inputs.test-command }}
      - name: Benchmark
        if: ${{ inputs.benchmark-command != '' }}
        run: ${{ inputs.benchmark-command }}
      - name: Plot
        if: ${{ inputs.plot-command != '' }}
        run: ${{ inputs.plot-command }}
      - name: Upload benchmark results
        if: ${{ (inputs.benchmark-command != '' || inputs.plot-command != '') && !cancelled() }}
        uses: actions/upload-artifact@v4
        with:
          name: std20-alpine319-amd64-benchmarks
          path: ${{ inputs.benchmark-artifact-path }}
          if-no-files-found: ignore
      - name: Publish benchmark results
        if: ${{ inputs.benchmark-publish-branch != '' && (inputs.benchmark-publish-job == '' || inputs.benchmark-publish-job == 'std20-alpine319-amd64') && success() && (github.event_name == 'push' || github.event_name == 'workflow_dispatch') }}
        shell: bash
        env:
          PUBLISH_BRANCH: ${{ inputs.benchmark-publish-branch }}
          SRC_DIR: ${{ inputs.benchmark-artifact-path }}
          GITHUB_TOKEN: ${{ github.token }}
          GITHUB_REPOSITORY: ${{ github.repository }}
        run: |
          set -euo pipefail
          if [ ! -d "$SRC_DIR" ]; then
            echo "No benchmark results at $SRC_DIR; skip publish"
            exit 0
          fi
          JOB_ID="${GITHUB_JOB}"
          ROOT="$GITHUB_WORKSPACE"
          cd "$ROOT"
          git config --global --add safe.directory "$ROOT"
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git remote set-url origin "https://x-access-token:${GITHUB_TOKEN}@github.com/${GITHUB_REPOSITORY}.git"
          WORK="$RUNNER_TEMP/bench-publish-$JOB_ID"
          cleanup_worktree() {
            cd "$ROOT"
            git worktree remove --force "$WORK" 2>/dev/null || true
            rm -rf "$WORK"
          }
          attempt=1
          while [ "$attempt" -le 12 ]; do
            echo "Publish attempt $attempt for $JOB_ID"
            cleanup_worktree
            mkdir -p "$WORK"
            if git fetch --depth 1 origin "$PUBLISH_BRANCH"; then
              git worktree add --force "$WORK" "FETCH_HEAD"
            else
              git worktree add --force --detach "$WORK"
              git -C "$WORK" checkout --orphan "$PUBLISH_BRANCH"
              git -C "$WORK" rm -rf . >/dev/null 2>&1 || true
            fi
            mkdir -p "$WORK/latest"
            rm -rf "$WORK/latest/$JOB_ID"
            mkdir -p "$WORK/latest/$JOB_ID"
            cp -a "$SRC_DIR"/. "$WORK/latest/$JOB_ID/"
            git -C "$WORK" add -A
            if git -C "$WORK" diff --cached --quiet; then
              echo "No benchmark publish changes for $JOB_ID"
              cleanup_worktree
              exit 0
            fi
            git -C "$WORK" commit -m "chore: update benchmark results for ${JOB_ID}"
            if git -C "$WORK" push origin "HEAD:$PUBLISH_BRANCH"; then
              echo "Published $JOB_ID to $PUBLISH_BRANCH/latest/$JOB_ID"
              cleanup_worktree
              exit 0
            fi
            echo "Push conflict; retrying..."
            cleanup_worktree
            attempt=$((attempt + 1))
            sleep $((attempt * 2))
          done
          echo "Failed to publish after retries"
          exit 1
      - name: Gather vcpkg logs
        if: failure()
        run: |
          mkdir -p build/vcpkg-logs
          if [ -d /tmp/vcpkg/buildtrees ]; then
            find /tmp/vcpkg/buildtrees -name "*.log" | while IFS= read -r f; do
              rel="${f#/tmp/vcpkg/}"
              mkdir -p "build/vcpkg-logs/$(dirname "$rel")"
              cp "$f" "build/vcpkg-logs/$rel"
            done
          fi
      - name: Upload logs on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: std20-alpine319-amd64-logs
          path: |
            build/CMakeFiles/CMakeOutput.log
            build/CMakeFiles/CMakeError.log
            build/Testing/Temporary/
            build/vcpkg-logs/
          if-no-files-found: ignore


  std20-alpine319-arm64:
    if: ${{ inputs.enable-std20-alpine319-arm64 }}
    runs-on: ubuntu-24.04-arm
    steps:
      - uses: actions/checkout@v4
      - name: Dump environment
        shell: bash
        run: |
          SCRIPT="$(mktemp)"
          echo '' | base64 -d > "$SCRIPT"
          docker run --rm \
            -v "$SCRIPT:/tmp/xci-step.sh:ro" \
            -v "$GITHUB_WORKSPACE:$GITHUB_WORKSPACE" \
            -v "$RUNNER_TEMP:$RUNNER_TEMP" \
            -v "$GITHUB_ENV:$GITHUB_ENV" \
            -v "$GITHUB_PATH:$GITHUB_PATH" \
            -e GITHUB_ENV -e GITHUB_PATH -e RUNNER_TEMP -e GITHUB_WORKSPACE -e GITHUB_JOB \
            -e VCPKG_ROOT -e VCPKG_CMAKE -e VCPKG_DEFAULT_BINARY_CACHE -e VCPKG_FORCE_SYSTEM_BINARIES \
            -e KMPKG_HOME -e KMPKG_CMAKE -e KMPKG_FORCE_SYSTEM_BINARIES \
            -w "$GITHUB_WORKSPACE" \
            ghcr.io/koomx/kumo-std20-alpine319-arm64:latest \
            sh /tmp/xci-step.sh
      - name: Install deps
        shell: bash
        env:
          XCI_DEPS: ${{ inputs.deps-alpine }}
        run: |
          SCRIPT="$(mktemp)"
          echo 'aWYgWyAtbiAiJFhDSV9ERVBTIiBdOyB0aGVuCiAgYXBrIGFkZCAtLW5vLWNhY2hlICRYQ0lfREVQUwpmaQo=' | base64 -d > "$SCRIPT"
          docker run --rm \
            -v "$SCRIPT:/tmp/xci-step.sh:ro" \
            -v "$GITHUB_WORKSPACE:$GITHUB_WORKSPACE" \
            -v "$RUNNER_TEMP:$RUNNER_TEMP" \
            -v "$GITHUB_ENV:$GITHUB_ENV" \
            -v "$GITHUB_PATH:$GITHUB_PATH" \
            -e GITHUB_ENV -e GITHUB_PATH -e RUNNER_TEMP -e GITHUB_WORKSPACE -e GITHUB_JOB \
            -e VCPKG_ROOT -e VCPKG_CMAKE -e VCPKG_DEFAULT_BINARY_CACHE -e VCPKG_FORCE_SYSTEM_BINARIES \
            -e KMPKG_HOME -e KMPKG_CMAKE -e KMPKG_FORCE_SYSTEM_BINARIES \
            -e XCI_DEPS \
            -w "$GITHUB_WORKSPACE" \
            ghcr.io/koomx/kumo-std20-alpine319-arm64:latest \
            sh /tmp/xci-step.sh
      - name: Bootstrap vcpkg
        shell: bash
        run: |
          SCRIPT="$(mktemp)"
          echo '' | base64 -d > "$SCRIPT"
          docker run --rm \
            -v "$SCRIPT:/tmp/xci-step.sh:ro" \
            -v "$GITHUB_WORKSPACE:$GITHUB_WORKSPACE" \
            -v "$RUNNER_TEMP:$RUNNER_TEMP" \
            -v "$GITHUB_ENV:$GITHUB_ENV" \
            -v "$GITHUB_PATH:$GITHUB_PATH" \
            -e GITHUB_ENV -e GITHUB_PATH -e RUNNER_TEMP -e GITHUB_WORKSPACE -e GITHUB_JOB \
            -e VCPKG_ROOT -e VCPKG_CMAKE -e VCPKG_DEFAULT_BINARY_CACHE -e VCPKG_FORCE_SYSTEM_BINARIES \
            -e KMPKG_HOME -e KMPKG_CMAKE -e KMPKG_FORCE_SYSTEM_BINARIES \
            -w "$GITHUB_WORKSPACE" \
            ghcr.io/koomx/kumo-std20-alpine319-arm64:latest \
            sh /tmp/xci-step.sh
      - name: Cache vcpkg binaries
        uses: actions/cache@v4
        with:
          path: ${{ runner.temp }}/vcpkg-binary-cache
          key: vcpkg-bin-std20-alpine319-arm64-${{ hashFiles('vcpkg.json', 'vcpkg-configuration.json', 'vcpkg-lock.json') }}
          restore-keys: |
            vcpkg-bin-std20-alpine319-arm64-
      - name: Configure
        shell: bash
        env:
          XCI_CMD: ${{ inputs.config-command }}
        run: |
          SCRIPT="$(mktemp)"
          printf '%s\n' "$XCI_CMD" > "$SCRIPT"
          docker run --rm \
            -v "$SCRIPT:/tmp/xci-step.sh:ro" \
            -v "$GITHUB_WORKSPACE:$GITHUB_WORKSPACE" \
            -v "$RUNNER_TEMP:$RUNNER_TEMP" \
            -v "$GITHUB_ENV:$GITHUB_ENV" \
            -v "$GITHUB_PATH:$GITHUB_PATH" \
            -e GITHUB_ENV -e GITHUB_PATH -e RUNNER_TEMP -e GITHUB_WORKSPACE -e GITHUB_JOB \
            -e VCPKG_ROOT -e VCPKG_CMAKE -e VCPKG_DEFAULT_BINARY_CACHE -e VCPKG_FORCE_SYSTEM_BINARIES \
            -e KMPKG_HOME -e KMPKG_CMAKE -e KMPKG_FORCE_SYSTEM_BINARIES \
            -w "$GITHUB_WORKSPACE" \
            ghcr.io/koomx/kumo-std20-alpine319-arm64:latest \
            sh /tmp/xci-step.sh
      - name: Build
        shell: bash
        env:
          XCI_CMD: ${{ inputs.build-command }}
        run: |
          SCRIPT="$(mktemp)"
          printf '%s\n' "$XCI_CMD" > "$SCRIPT"
          docker run --rm \
            -v "$SCRIPT:/tmp/xci-step.sh:ro" \
            -v "$GITHUB_WORKSPACE:$GITHUB_WORKSPACE" \
            -v "$RUNNER_TEMP:$RUNNER_TEMP" \
            -v "$GITHUB_ENV:$GITHUB_ENV" \
            -v "$GITHUB_PATH:$GITHUB_PATH" \
            -e GITHUB_ENV -e GITHUB_PATH -e RUNNER_TEMP -e GITHUB_WORKSPACE -e GITHUB_JOB \
            -e VCPKG_ROOT -e VCPKG_CMAKE -e VCPKG_DEFAULT_BINARY_CACHE -e VCPKG_FORCE_SYSTEM_BINARIES \
            -e KMPKG_HOME -e KMPKG_CMAKE -e KMPKG_FORCE_SYSTEM_BINARIES \
            -w "$GITHUB_WORKSPACE" \
            ghcr.io/koomx/kumo-std20-alpine319-arm64:latest \
            sh /tmp/xci-step.sh
      - name: Test
        if: ${{ inputs.test-command != '' }}
        shell: bash
        env:
          XCI_CMD: ${{ inputs.test-command }}
        run: |
          SCRIPT="$(mktemp)"
          printf '%s\n' "$XCI_CMD" > "$SCRIPT"
          docker run --rm \
            -v "$SCRIPT:/tmp/xci-step.sh:ro" \
            -v "$GITHUB_WORKSPACE:$GITHUB_WORKSPACE" \
            -v "$RUNNER_TEMP:$RUNNER_TEMP" \
            -v "$GITHUB_ENV:$GITHUB_ENV" \
            -v "$GITHUB_PATH:$GITHUB_PATH" \
            -e GITHUB_ENV -e GITHUB_PATH -e RUNNER_TEMP -e GITHUB_WORKSPACE -e GITHUB_JOB \
            -e VCPKG_ROOT -e VCPKG_CMAKE -e VCPKG_DEFAULT_BINARY_CACHE -e VCPKG_FORCE_SYSTEM_BINARIES \
            -e KMPKG_HOME -e KMPKG_CMAKE -e KMPKG_FORCE_SYSTEM_BINARIES \
            -w "$GITHUB_WORKSPACE" \
            ghcr.io/koomx/kumo-std20-alpine319-arm64:latest \
            sh /tmp/xci-step.sh
      - name: Benchmark
        if: ${{ inputs.benchmark-command != '' }}
        shell: bash
        env:
          XCI_CMD: ${{ inputs.benchmark-command }}
        run: |
          SCRIPT="$(mktemp)"
          printf '%s\n' "$XCI_CMD" > "$SCRIPT"
          docker run --rm \
            -v "$SCRIPT:/tmp/xci-step.sh:ro" \
            -v "$GITHUB_WORKSPACE:$GITHUB_WORKSPACE" \
            -v "$RUNNER_TEMP:$RUNNER_TEMP" \
            -v "$GITHUB_ENV:$GITHUB_ENV" \
            -v "$GITHUB_PATH:$GITHUB_PATH" \
            -e GITHUB_ENV -e GITHUB_PATH -e RUNNER_TEMP -e GITHUB_WORKSPACE -e GITHUB_JOB \
            -e VCPKG_ROOT -e VCPKG_CMAKE -e VCPKG_DEFAULT_BINARY_CACHE -e VCPKG_FORCE_SYSTEM_BINARIES \
            -e KMPKG_HOME -e KMPKG_CMAKE -e KMPKG_FORCE_SYSTEM_BINARIES \
            -w "$GITHUB_WORKSPACE" \
            ghcr.io/koomx/kumo-std20-alpine319-arm64:latest \
            sh /tmp/xci-step.sh
      - name: Plot
        if: ${{ inputs.plot-command != '' }}
        shell: bash
        env:
          XCI_CMD: ${{ inputs.plot-command }}
        run: |
          SCRIPT="$(mktemp)"
          printf '%s\n' "$XCI_CMD" > "$SCRIPT"
          docker run --rm \
            -v "$SCRIPT:/tmp/xci-step.sh:ro" \
            -v "$GITHUB_WORKSPACE:$GITHUB_WORKSPACE" \
            -v "$RUNNER_TEMP:$RUNNER_TEMP" \
            -v "$GITHUB_ENV:$GITHUB_ENV" \
            -v "$GITHUB_PATH:$GITHUB_PATH" \
            -e GITHUB_ENV -e GITHUB_PATH -e RUNNER_TEMP -e GITHUB_WORKSPACE -e GITHUB_JOB \
            -e VCPKG_ROOT -e VCPKG_CMAKE -e VCPKG_DEFAULT_BINARY_CACHE -e VCPKG_FORCE_SYSTEM_BINARIES \
            -e KMPKG_HOME -e KMPKG_CMAKE -e KMPKG_FORCE_SYSTEM_BINARIES \
            -w "$GITHUB_WORKSPACE" \
            ghcr.io/koomx/kumo-std20-alpine319-arm64:latest \
            sh /tmp/xci-step.sh
      - name: Upload benchmark results
        if: ${{ (inputs.benchmark-command != '' || inputs.plot-command != '') && !cancelled() }}
        uses: actions/upload-artifact@v4
        with:
          name: std20-alpine319-arm64-benchmarks
          path: ${{ inputs.benchmark-artifact-path }}
          if-no-files-found: ignore
      - name: Publish benchmark results
        if: ${{ inputs.benchmark-publish-branch != '' && (inputs.benchmark-publish-job == '' || inputs.benchmark-publish-job == 'std20-alpine319-arm64') && success() && (github.event_name == 'push' || github.event_name == 'workflow_dispatch') }}
        shell: bash
        env:
          PUBLISH_BRANCH: ${{ inputs.benchmark-publish-branch }}
          SRC_DIR: ${{ inputs.benchmark-artifact-path }}
          GITHUB_TOKEN: ${{ github.token }}
          GITHUB_REPOSITORY: ${{ github.repository }}
        run: |
          set -euo pipefail
          if [ ! -d "$SRC_DIR" ]; then
            echo "No benchmark results at $SRC_DIR; skip publish"
            exit 0
          fi
          JOB_ID="${GITHUB_JOB}"
          ROOT="$GITHUB_WORKSPACE"
          cd "$ROOT"
          git config --global --add safe.directory "$ROOT"
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git remote set-url origin "https://x-access-token:${GITHUB_TOKEN}@github.com/${GITHUB_REPOSITORY}.git"
          WORK="$RUNNER_TEMP/bench-publish-$JOB_ID"
          cleanup_worktree() {
            cd "$ROOT"
            git worktree remove --force "$WORK" 2>/dev/null || true
            rm -rf "$WORK"
          }
          attempt=1
          while [ "$attempt" -le 12 ]; do
            echo "Publish attempt $attempt for $JOB_ID"
            cleanup_worktree
            mkdir -p "$WORK"
            if git fetch --depth 1 origin "$PUBLISH_BRANCH"; then
              git worktree add --force "$WORK" "FETCH_HEAD"
            else
              git worktree add --force --detach "$WORK"
              git -C "$WORK" checkout --orphan "$PUBLISH_BRANCH"
              git -C "$WORK" rm -rf . >/dev/null 2>&1 || true
            fi
            mkdir -p "$WORK/latest"
            rm -rf "$WORK/latest/$JOB_ID"
            mkdir -p "$WORK/latest/$JOB_ID"
            cp -a "$SRC_DIR"/. "$WORK/latest/$JOB_ID/"
            git -C "$WORK" add -A
            if git -C "$WORK" diff --cached --quiet; then
              echo "No benchmark publish changes for $JOB_ID"
              cleanup_worktree
              exit 0
            fi
            git -C "$WORK" commit -m "chore: update benchmark results for ${JOB_ID}"
            if git -C "$WORK" push origin "HEAD:$PUBLISH_BRANCH"; then
              echo "Published $JOB_ID to $PUBLISH_BRANCH/latest/$JOB_ID"
              cleanup_worktree
              exit 0
            fi
            echo "Push conflict; retrying..."
            cleanup_worktree
            attempt=$((attempt + 1))
            sleep $((attempt * 2))
          done
          echo "Failed to publish after retries"
          exit 1
      - name: Gather logs
        if: failure()
        shell: bash
        run: |
          SCRIPT="$(mktemp)"
          echo '' | base64 -d > "$SCRIPT"
          docker run --rm \
            -v "$SCRIPT:/tmp/xci-step.sh:ro" \
            -v "$GITHUB_WORKSPACE:$GITHUB_WORKSPACE" \
            -v "$RUNNER_TEMP:$RUNNER_TEMP" \
            -v "$GITHUB_ENV:$GITHUB_ENV" \
            -v "$GITHUB_PATH:$GITHUB_PATH" \
            -e GITHUB_ENV -e GITHUB_PATH -e RUNNER_TEMP -e GITHUB_WORKSPACE -e GITHUB_JOB \
            -e VCPKG_ROOT -e VCPKG_CMAKE -e VCPKG_DEFAULT_BINARY_CACHE -e VCPKG_FORCE_SYSTEM_BINARIES \
            -e KMPKG_HOME -e KMPKG_CMAKE -e KMPKG_FORCE_SYSTEM_BINARIES \
            -w "$GITHUB_WORKSPACE" \
            ghcr.io/koomx/kumo-std20-alpine319-arm64:latest \
            sh /tmp/xci-step.sh
      - name: Upload logs on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: std20-alpine319-arm64-logs
          path: |
            build/CMakeFiles/CMakeOutput.log
            build/CMakeFiles/CMakeError.log
            build/Testing/Temporary/
            build/vcpkg-logs/
          if-no-files-found: ignore

  std20-alpine320-amd64:
    if: ${{ inputs.enable-std20-alpine320-amd64 }}
    runs-on: ubuntu-latest
    container: ghcr.io/koomx/kumo-std20-alpine320-amd64:latest
    steps:
      - uses: actions/checkout@v4
      - name: Dump environment
        run: |
          echo "=== Environment Dump ==="
          if [ -f /etc/os-release ]; then
              . /etc/os-release
              echo "OS: $NAME $VERSION ($(uname -m))"
          elif [ "$(uname)" = "Darwin" ]; then
              echo "OS: macOS $(sw_vers -productVersion 2>/dev/null || echo unknown) ($(uname -m))"
          else
              echo "OS: $(uname -a 2>/dev/null || echo unknown)"
          fi
          for tool in cmake ninja make git python3 curl tar gzip; do
              if command -v $tool &>/dev/null; then
                  echo "  $tool: $(command -v $tool)  ($($tool --version 2>&1 | head -1))"
              else
                  echo "  $tool: NOT FOUND"
              fi
          done
          for cc in gcc g++ clang clang++ cc c++; do
              if command -v $cc &>/dev/null; then
                  echo "  $($cc --version 2>&1 | head -1)"
                  break
              fi
          done
          echo "=== End ==="
      - name: Install deps
        run: |
          if [ -n "${{ inputs.deps-alpine }}" ]; then
            apk add --no-cache ${{ inputs.deps-alpine }}
          fi
      - name: Bootstrap vcpkg
        run: |
          git clone --depth 1 --branch master https://github.com/microsoft/vcpkg.git /tmp/vcpkg
          /tmp/vcpkg/bootstrap-vcpkg.sh -disableMetrics
          echo "VCPKG_ROOT=/tmp/vcpkg" >> "$GITHUB_ENV"
          echo "VCPKG_CMAKE=/tmp/vcpkg/scripts/buildsystems/vcpkg.cmake" >> "$GITHUB_ENV"
          echo "VCPKG_FORCE_SYSTEM_BINARIES=1" >> "$GITHUB_ENV"
          mkdir -p "$RUNNER_TEMP/vcpkg-binary-cache"
          echo "VCPKG_DEFAULT_BINARY_CACHE=$RUNNER_TEMP/vcpkg-binary-cache" >> "$GITHUB_ENV"
      - name: Cache vcpkg binaries
        uses: actions/cache@v4
        with:
          path: ${{ runner.temp }}/vcpkg-binary-cache
          key: vcpkg-bin-std20-alpine320-amd64-${{ hashFiles('vcpkg.json', 'vcpkg-configuration.json', 'vcpkg-lock.json') }}
          restore-keys: |
            vcpkg-bin-std20-alpine320-amd64-
      - name: Configure
        run: ${{ inputs.config-command }}
      - name: Build
        run: ${{ inputs.build-command }}
      - name: Test
        if: ${{ inputs.test-command != '' }}
        run: ${{ inputs.test-command }}
      - name: Benchmark
        if: ${{ inputs.benchmark-command != '' }}
        run: ${{ inputs.benchmark-command }}
      - name: Plot
        if: ${{ inputs.plot-command != '' }}
        run: ${{ inputs.plot-command }}
      - name: Upload benchmark results
        if: ${{ (inputs.benchmark-command != '' || inputs.plot-command != '') && !cancelled() }}
        uses: actions/upload-artifact@v4
        with:
          name: std20-alpine320-amd64-benchmarks
          path: ${{ inputs.benchmark-artifact-path }}
          if-no-files-found: ignore
      - name: Publish benchmark results
        if: ${{ inputs.benchmark-publish-branch != '' && (inputs.benchmark-publish-job == '' || inputs.benchmark-publish-job == 'std20-alpine320-amd64') && success() && (github.event_name == 'push' || github.event_name == 'workflow_dispatch') }}
        shell: bash
        env:
          PUBLISH_BRANCH: ${{ inputs.benchmark-publish-branch }}
          SRC_DIR: ${{ inputs.benchmark-artifact-path }}
          GITHUB_TOKEN: ${{ github.token }}
          GITHUB_REPOSITORY: ${{ github.repository }}
        run: |
          set -euo pipefail
          if [ ! -d "$SRC_DIR" ]; then
            echo "No benchmark results at $SRC_DIR; skip publish"
            exit 0
          fi
          JOB_ID="${GITHUB_JOB}"
          ROOT="$GITHUB_WORKSPACE"
          cd "$ROOT"
          git config --global --add safe.directory "$ROOT"
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git remote set-url origin "https://x-access-token:${GITHUB_TOKEN}@github.com/${GITHUB_REPOSITORY}.git"
          WORK="$RUNNER_TEMP/bench-publish-$JOB_ID"
          cleanup_worktree() {
            cd "$ROOT"
            git worktree remove --force "$WORK" 2>/dev/null || true
            rm -rf "$WORK"
          }
          attempt=1
          while [ "$attempt" -le 12 ]; do
            echo "Publish attempt $attempt for $JOB_ID"
            cleanup_worktree
            mkdir -p "$WORK"
            if git fetch --depth 1 origin "$PUBLISH_BRANCH"; then
              git worktree add --force "$WORK" "FETCH_HEAD"
            else
              git worktree add --force --detach "$WORK"
              git -C "$WORK" checkout --orphan "$PUBLISH_BRANCH"
              git -C "$WORK" rm -rf . >/dev/null 2>&1 || true
            fi
            mkdir -p "$WORK/latest"
            rm -rf "$WORK/latest/$JOB_ID"
            mkdir -p "$WORK/latest/$JOB_ID"
            cp -a "$SRC_DIR"/. "$WORK/latest/$JOB_ID/"
            git -C "$WORK" add -A
            if git -C "$WORK" diff --cached --quiet; then
              echo "No benchmark publish changes for $JOB_ID"
              cleanup_worktree
              exit 0
            fi
            git -C "$WORK" commit -m "chore: update benchmark results for ${JOB_ID}"
            if git -C "$WORK" push origin "HEAD:$PUBLISH_BRANCH"; then
              echo "Published $JOB_ID to $PUBLISH_BRANCH/latest/$JOB_ID"
              cleanup_worktree
              exit 0
            fi
            echo "Push conflict; retrying..."
            cleanup_worktree
            attempt=$((attempt + 1))
            sleep $((attempt * 2))
          done
          echo "Failed to publish after retries"
          exit 1
      - name: Gather vcpkg logs
        if: failure()
        run: |
          mkdir -p build/vcpkg-logs
          if [ -d /tmp/vcpkg/buildtrees ]; then
            find /tmp/vcpkg/buildtrees -name "*.log" | while IFS= read -r f; do
              rel="${f#/tmp/vcpkg/}"
              mkdir -p "build/vcpkg-logs/$(dirname "$rel")"
              cp "$f" "build/vcpkg-logs/$rel"
            done
          fi
      - name: Upload logs on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: std20-alpine320-amd64-logs
          path: |
            build/CMakeFiles/CMakeOutput.log
            build/CMakeFiles/CMakeError.log
            build/Testing/Temporary/
            build/vcpkg-logs/
          if-no-files-found: ignore


  std20-alpine320-arm64:
    if: ${{ inputs.enable-std20-alpine320-arm64 }}
    runs-on: ubuntu-24.04-arm
    steps:
      - uses: actions/checkout@v4
      - name: Dump environment
        shell: bash
        run: |
          SCRIPT="$(mktemp)"
          echo '' | base64 -d > "$SCRIPT"
          docker run --rm \
            -v "$SCRIPT:/tmp/xci-step.sh:ro" \
            -v "$GITHUB_WORKSPACE:$GITHUB_WORKSPACE" \
            -v "$RUNNER_TEMP:$RUNNER_TEMP" \
            -v "$GITHUB_ENV:$GITHUB_ENV" \
            -v "$GITHUB_PATH:$GITHUB_PATH" \
            -e GITHUB_ENV -e GITHUB_PATH -e RUNNER_TEMP -e GITHUB_WORKSPACE -e GITHUB_JOB \
            -e VCPKG_ROOT -e VCPKG_CMAKE -e VCPKG_DEFAULT_BINARY_CACHE -e VCPKG_FORCE_SYSTEM_BINARIES \
            -e KMPKG_HOME -e KMPKG_CMAKE -e KMPKG_FORCE_SYSTEM_BINARIES \
            -w "$GITHUB_WORKSPACE" \
            ghcr.io/koomx/kumo-std20-alpine320-arm64:latest \
            sh /tmp/xci-step.sh
      - name: Install deps
        shell: bash
        env:
          XCI_DEPS: ${{ inputs.deps-alpine }}
        run: |
          SCRIPT="$(mktemp)"
          echo 'aWYgWyAtbiAiJFhDSV9ERVBTIiBdOyB0aGVuCiAgYXBrIGFkZCAtLW5vLWNhY2hlICRYQ0lfREVQUwpmaQo=' | base64 -d > "$SCRIPT"
          docker run --rm \
            -v "$SCRIPT:/tmp/xci-step.sh:ro" \
            -v "$GITHUB_WORKSPACE:$GITHUB_WORKSPACE" \
            -v "$RUNNER_TEMP:$RUNNER_TEMP" \
            -v "$GITHUB_ENV:$GITHUB_ENV" \
            -v "$GITHUB_PATH:$GITHUB_PATH" \
            -e GITHUB_ENV -e GITHUB_PATH -e RUNNER_TEMP -e GITHUB_WORKSPACE -e GITHUB_JOB \
            -e VCPKG_ROOT -e VCPKG_CMAKE -e VCPKG_DEFAULT_BINARY_CACHE -e VCPKG_FORCE_SYSTEM_BINARIES \
            -e KMPKG_HOME -e KMPKG_CMAKE -e KMPKG_FORCE_SYSTEM_BINARIES \
            -e XCI_DEPS \
            -w "$GITHUB_WORKSPACE" \
            ghcr.io/koomx/kumo-std20-alpine320-arm64:latest \
            sh /tmp/xci-step.sh
      - name: Bootstrap vcpkg
        shell: bash
        run: |
          SCRIPT="$(mktemp)"
          echo '' | base64 -d > "$SCRIPT"
          docker run --rm \
            -v "$SCRIPT:/tmp/xci-step.sh:ro" \
            -v "$GITHUB_WORKSPACE:$GITHUB_WORKSPACE" \
            -v "$RUNNER_TEMP:$RUNNER_TEMP" \
            -v "$GITHUB_ENV:$GITHUB_ENV" \
            -v "$GITHUB_PATH:$GITHUB_PATH" \
            -e GITHUB_ENV -e GITHUB_PATH -e RUNNER_TEMP -e GITHUB_WORKSPACE -e GITHUB_JOB \
            -e VCPKG_ROOT -e VCPKG_CMAKE -e VCPKG_DEFAULT_BINARY_CACHE -e VCPKG_FORCE_SYSTEM_BINARIES \
            -e KMPKG_HOME -e KMPKG_CMAKE -e KMPKG_FORCE_SYSTEM_BINARIES \
            -w "$GITHUB_WORKSPACE" \
            ghcr.io/koomx/kumo-std20-alpine320-arm64:latest \
            sh /tmp/xci-step.sh
      - name: Cache vcpkg binaries
        uses: actions/cache@v4
        with:
          path: ${{ runner.temp }}/vcpkg-binary-cache
          key: vcpkg-bin-std20-alpine320-arm64-${{ hashFiles('vcpkg.json', 'vcpkg-configuration.json', 'vcpkg-lock.json') }}
          restore-keys: |
            vcpkg-bin-std20-alpine320-arm64-
      - name: Configure
        shell: bash
        env:
          XCI_CMD: ${{ inputs.config-command }}
        run: |
          SCRIPT="$(mktemp)"
          printf '%s\n' "$XCI_CMD" > "$SCRIPT"
          docker run --rm \
            -v "$SCRIPT:/tmp/xci-step.sh:ro" \
            -v "$GITHUB_WORKSPACE:$GITHUB_WORKSPACE" \
            -v "$RUNNER_TEMP:$RUNNER_TEMP" \
            -v "$GITHUB_ENV:$GITHUB_ENV" \
            -v "$GITHUB_PATH:$GITHUB_PATH" \
            -e GITHUB_ENV -e GITHUB_PATH -e RUNNER_TEMP -e GITHUB_WORKSPACE -e GITHUB_JOB \
            -e VCPKG_ROOT -e VCPKG_CMAKE -e VCPKG_DEFAULT_BINARY_CACHE -e VCPKG_FORCE_SYSTEM_BINARIES \
            -e KMPKG_HOME -e KMPKG_CMAKE -e KMPKG_FORCE_SYSTEM_BINARIES \
            -w "$GITHUB_WORKSPACE" \
            ghcr.io/koomx/kumo-std20-alpine320-arm64:latest \
            sh /tmp/xci-step.sh
      - name: Build
        shell: bash
        env:
          XCI_CMD: ${{ inputs.build-command }}
        run: |
          SCRIPT="$(mktemp)"
          printf '%s\n' "$XCI_CMD" > "$SCRIPT"
          docker run --rm \
            -v "$SCRIPT:/tmp/xci-step.sh:ro" \
            -v "$GITHUB_WORKSPACE:$GITHUB_WORKSPACE" \
            -v "$RUNNER_TEMP:$RUNNER_TEMP" \
            -v "$GITHUB_ENV:$GITHUB_ENV" \
            -v "$GITHUB_PATH:$GITHUB_PATH" \
            -e GITHUB_ENV -e GITHUB_PATH -e RUNNER_TEMP -e GITHUB_WORKSPACE -e GITHUB_JOB \
            -e VCPKG_ROOT -e VCPKG_CMAKE -e VCPKG_DEFAULT_BINARY_CACHE -e VCPKG_FORCE_SYSTEM_BINARIES \
            -e KMPKG_HOME -e KMPKG_CMAKE -e KMPKG_FORCE_SYSTEM_BINARIES \
            -w "$GITHUB_WORKSPACE" \
            ghcr.io/koomx/kumo-std20-alpine320-arm64:latest \
            sh /tmp/xci-step.sh
      - name: Test
        if: ${{ inputs.test-command != '' }}
        shell: bash
        env:
          XCI_CMD: ${{ inputs.test-command }}
        run: |
          SCRIPT="$(mktemp)"
          printf '%s\n' "$XCI_CMD" > "$SCRIPT"
          docker run --rm \
            -v "$SCRIPT:/tmp/xci-step.sh:ro" \
            -v "$GITHUB_WORKSPACE:$GITHUB_WORKSPACE" \
            -v "$RUNNER_TEMP:$RUNNER_TEMP" \
            -v "$GITHUB_ENV:$GITHUB_ENV" \
            -v "$GITHUB_PATH:$GITHUB_PATH" \
            -e GITHUB_ENV -e GITHUB_PATH -e RUNNER_TEMP -e GITHUB_WORKSPACE -e GITHUB_JOB \
            -e VCPKG_ROOT -e VCPKG_CMAKE -e VCPKG_DEFAULT_BINARY_CACHE -e VCPKG_FORCE_SYSTEM_BINARIES \
            -e KMPKG_HOME -e KMPKG_CMAKE -e KMPKG_FORCE_SYSTEM_BINARIES \
            -w "$GITHUB_WORKSPACE" \
            ghcr.io/koomx/kumo-std20-alpine320-arm64:latest \
            sh /tmp/xci-step.sh
      - name: Benchmark
        if: ${{ inputs.benchmark-command != '' }}
        shell: bash
        env:
          XCI_CMD: ${{ inputs.benchmark-command }}
        run: |
          SCRIPT="$(mktemp)"
          printf '%s\n' "$XCI_CMD" > "$SCRIPT"
          docker run --rm \
            -v "$SCRIPT:/tmp/xci-step.sh:ro" \
            -v "$GITHUB_WORKSPACE:$GITHUB_WORKSPACE" \
            -v "$RUNNER_TEMP:$RUNNER_TEMP" \
            -v "$GITHUB_ENV:$GITHUB_ENV" \
            -v "$GITHUB_PATH:$GITHUB_PATH" \
            -e GITHUB_ENV -e GITHUB_PATH -e RUNNER_TEMP -e GITHUB_WORKSPACE -e GITHUB_JOB \
            -e VCPKG_ROOT -e VCPKG_CMAKE -e VCPKG_DEFAULT_BINARY_CACHE -e VCPKG_FORCE_SYSTEM_BINARIES \
            -e KMPKG_HOME -e KMPKG_CMAKE -e KMPKG_FORCE_SYSTEM_BINARIES \
            -w "$GITHUB_WORKSPACE" \
            ghcr.io/koomx/kumo-std20-alpine320-arm64:latest \
            sh /tmp/xci-step.sh
      - name: Plot
        if: ${{ inputs.plot-command != '' }}
        shell: bash
        env:
          XCI_CMD: ${{ inputs.plot-command }}
        run: |
          SCRIPT="$(mktemp)"
          printf '%s\n' "$XCI_CMD" > "$SCRIPT"
          docker run --rm \
            -v "$SCRIPT:/tmp/xci-step.sh:ro" \
            -v "$GITHUB_WORKSPACE:$GITHUB_WORKSPACE" \
            -v "$RUNNER_TEMP:$RUNNER_TEMP" \
            -v "$GITHUB_ENV:$GITHUB_ENV" \
            -v "$GITHUB_PATH:$GITHUB_PATH" \
            -e GITHUB_ENV -e GITHUB_PATH -e RUNNER_TEMP -e GITHUB_WORKSPACE -e GITHUB_JOB \
            -e VCPKG_ROOT -e VCPKG_CMAKE -e VCPKG_DEFAULT_BINARY_CACHE -e VCPKG_FORCE_SYSTEM_BINARIES \
            -e KMPKG_HOME -e KMPKG_CMAKE -e KMPKG_FORCE_SYSTEM_BINARIES \
            -w "$GITHUB_WORKSPACE" \
            ghcr.io/koomx/kumo-std20-alpine320-arm64:latest \
            sh /tmp/xci-step.sh
      - name: Upload benchmark results
        if: ${{ (inputs.benchmark-command != '' || inputs.plot-command != '') && !cancelled() }}
        uses: actions/upload-artifact@v4
        with:
          name: std20-alpine320-arm64-benchmarks
          path: ${{ inputs.benchmark-artifact-path }}
          if-no-files-found: ignore
      - name: Publish benchmark results
        if: ${{ inputs.benchmark-publish-branch != '' && (inputs.benchmark-publish-job == '' || inputs.benchmark-publish-job == 'std20-alpine320-arm64') && success() && (github.event_name == 'push' || github.event_name == 'workflow_dispatch') }}
        shell: bash
        env:
          PUBLISH_BRANCH: ${{ inputs.benchmark-publish-branch }}
          SRC_DIR: ${{ inputs.benchmark-artifact-path }}
          GITHUB_TOKEN: ${{ github.token }}
          GITHUB_REPOSITORY: ${{ github.repository }}
        run: |
          set -euo pipefail
          if [ ! -d "$SRC_DIR" ]; then
            echo "No benchmark results at $SRC_DIR; skip publish"
            exit 0
          fi
          JOB_ID="${GITHUB_JOB}"
          ROOT="$GITHUB_WORKSPACE"
          cd "$ROOT"
          git config --global --add safe.directory "$ROOT"
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git remote set-url origin "https://x-access-token:${GITHUB_TOKEN}@github.com/${GITHUB_REPOSITORY}.git"
          WORK="$RUNNER_TEMP/bench-publish-$JOB_ID"
          cleanup_worktree() {
            cd "$ROOT"
            git worktree remove --force "$WORK" 2>/dev/null || true
            rm -rf "$WORK"
          }
          attempt=1
          while [ "$attempt" -le 12 ]; do
            echo "Publish attempt $attempt for $JOB_ID"
            cleanup_worktree
            mkdir -p "$WORK"
            if git fetch --depth 1 origin "$PUBLISH_BRANCH"; then
              git worktree add --force "$WORK" "FETCH_HEAD"
            else
              git worktree add --force --detach "$WORK"
              git -C "$WORK" checkout --orphan "$PUBLISH_BRANCH"
              git -C "$WORK" rm -rf . >/dev/null 2>&1 || true
            fi
            mkdir -p "$WORK/latest"
            rm -rf "$WORK/latest/$JOB_ID"
            mkdir -p "$WORK/latest/$JOB_ID"
            cp -a "$SRC_DIR"/. "$WORK/latest/$JOB_ID/"
            git -C "$WORK" add -A
            if git -C "$WORK" diff --cached --quiet; then
              echo "No benchmark publish changes for $JOB_ID"
              cleanup_worktree
              exit 0
            fi
            git -C "$WORK" commit -m "chore: update benchmark results for ${JOB_ID}"
            if git -C "$WORK" push origin "HEAD:$PUBLISH_BRANCH"; then
              echo "Published $JOB_ID to $PUBLISH_BRANCH/latest/$JOB_ID"
              cleanup_worktree
              exit 0
            fi
            echo "Push conflict; retrying..."
            cleanup_worktree
            attempt=$((attempt + 1))
            sleep $((attempt * 2))
          done
          echo "Failed to publish after retries"
          exit 1
      - name: Gather logs
        if: failure()
        shell: bash
        run: |
          SCRIPT="$(mktemp)"
          echo '' | base64 -d > "$SCRIPT"
          docker run --rm \
            -v "$SCRIPT:/tmp/xci-step.sh:ro" \
            -v "$GITHUB_WORKSPACE:$GITHUB_WORKSPACE" \
            -v "$RUNNER_TEMP:$RUNNER_TEMP" \
            -v "$GITHUB_ENV:$GITHUB_ENV" \
            -v "$GITHUB_PATH:$GITHUB_PATH" \
            -e GITHUB_ENV -e GITHUB_PATH -e RUNNER_TEMP -e GITHUB_WORKSPACE -e GITHUB_JOB \
            -e VCPKG_ROOT -e VCPKG_CMAKE -e VCPKG_DEFAULT_BINARY_CACHE -e VCPKG_FORCE_SYSTEM_BINARIES \
            -e KMPKG_HOME -e KMPKG_CMAKE -e KMPKG_FORCE_SYSTEM_BINARIES \
            -w "$GITHUB_WORKSPACE" \
            ghcr.io/koomx/kumo-std20-alpine320-arm64:latest \
            sh /tmp/xci-step.sh
      - name: Upload logs on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: std20-alpine320-arm64-logs
          path: |
            build/CMakeFiles/CMakeOutput.log
            build/CMakeFiles/CMakeError.log
            build/Testing/Temporary/
            build/vcpkg-logs/
          if-no-files-found: ignore

  std20-centos9-amd64:
    if: ${{ inputs.enable-std20-centos9-amd64 }}
    runs-on: ubuntu-latest
    container: ghcr.io/koomx/kumo-std20-centos9-amd64:latest
    steps:
      - uses: actions/checkout@v4
      - name: Dump environment
        run: |
          echo "=== Environment Dump ==="
          if [ -f /etc/os-release ]; then
              . /etc/os-release
              echo "OS: $NAME $VERSION ($(uname -m))"
          elif [ "$(uname)" = "Darwin" ]; then
              echo "OS: macOS $(sw_vers -productVersion 2>/dev/null || echo unknown) ($(uname -m))"
          else
              echo "OS: $(uname -a 2>/dev/null || echo unknown)"
          fi
          for tool in cmake ninja make git python3 curl tar gzip; do
              if command -v $tool &>/dev/null; then
                  echo "  $tool: $(command -v $tool)  ($($tool --version 2>&1 | head -1))"
              else
                  echo "  $tool: NOT FOUND"
              fi
          done
          for cc in gcc g++ clang clang++ cc c++; do
              if command -v $cc &>/dev/null; then
                  echo "  $($cc --version 2>&1 | head -1)"
                  break
              fi
          done
          echo "=== End ==="
      - name: Install deps
        run: |
          if [ -n "${{ inputs.deps-centos }}" ]; then
            dnf install -y ${{ inputs.deps-centos }}
          fi
      - name: Bootstrap vcpkg
        run: |
          git clone --depth 1 --branch master https://github.com/microsoft/vcpkg.git /tmp/vcpkg
          /tmp/vcpkg/bootstrap-vcpkg.sh -disableMetrics
          echo "VCPKG_ROOT=/tmp/vcpkg" >> "$GITHUB_ENV"
          echo "VCPKG_CMAKE=/tmp/vcpkg/scripts/buildsystems/vcpkg.cmake" >> "$GITHUB_ENV"
          echo "VCPKG_FORCE_SYSTEM_BINARIES=1" >> "$GITHUB_ENV"
          mkdir -p "$RUNNER_TEMP/vcpkg-binary-cache"
          echo "VCPKG_DEFAULT_BINARY_CACHE=$RUNNER_TEMP/vcpkg-binary-cache" >> "$GITHUB_ENV"
      - name: Cache vcpkg binaries
        uses: actions/cache@v4
        with:
          path: ${{ runner.temp }}/vcpkg-binary-cache
          key: vcpkg-bin-std20-centos9-amd64-${{ hashFiles('vcpkg.json', 'vcpkg-configuration.json', 'vcpkg-lock.json') }}
          restore-keys: |
            vcpkg-bin-std20-centos9-amd64-
      - name: Configure
        run: ${{ inputs.config-command }}
      - name: Build
        run: ${{ inputs.build-command }}
      - name: Test
        if: ${{ inputs.test-command != '' }}
        run: ${{ inputs.test-command }}
      - name: Benchmark
        if: ${{ inputs.benchmark-command != '' }}
        run: ${{ inputs.benchmark-command }}
      - name: Plot
        if: ${{ inputs.plot-command != '' }}
        run: ${{ inputs.plot-command }}
      - name: Upload benchmark results
        if: ${{ (inputs.benchmark-command != '' || inputs.plot-command != '') && !cancelled() }}
        uses: actions/upload-artifact@v4
        with:
          name: std20-centos9-amd64-benchmarks
          path: ${{ inputs.benchmark-artifact-path }}
          if-no-files-found: ignore
      - name: Publish benchmark results
        if: ${{ inputs.benchmark-publish-branch != '' && (inputs.benchmark-publish-job == '' || inputs.benchmark-publish-job == 'std20-centos9-amd64') && success() && (github.event_name == 'push' || github.event_name == 'workflow_dispatch') }}
        shell: bash
        env:
          PUBLISH_BRANCH: ${{ inputs.benchmark-publish-branch }}
          SRC_DIR: ${{ inputs.benchmark-artifact-path }}
          GITHUB_TOKEN: ${{ github.token }}
          GITHUB_REPOSITORY: ${{ github.repository }}
        run: |
          set -euo pipefail
          if [ ! -d "$SRC_DIR" ]; then
            echo "No benchmark results at $SRC_DIR; skip publish"
            exit 0
          fi
          JOB_ID="${GITHUB_JOB}"
          ROOT="$GITHUB_WORKSPACE"
          cd "$ROOT"
          git config --global --add safe.directory "$ROOT"
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git remote set-url origin "https://x-access-token:${GITHUB_TOKEN}@github.com/${GITHUB_REPOSITORY}.git"
          WORK="$RUNNER_TEMP/bench-publish-$JOB_ID"
          cleanup_worktree() {
            cd "$ROOT"
            git worktree remove --force "$WORK" 2>/dev/null || true
            rm -rf "$WORK"
          }
          attempt=1
          while [ "$attempt" -le 12 ]; do
            echo "Publish attempt $attempt for $JOB_ID"
            cleanup_worktree
            mkdir -p "$WORK"
            if git fetch --depth 1 origin "$PUBLISH_BRANCH"; then
              git worktree add --force "$WORK" "FETCH_HEAD"
            else
              git worktree add --force --detach "$WORK"
              git -C "$WORK" checkout --orphan "$PUBLISH_BRANCH"
              git -C "$WORK" rm -rf . >/dev/null 2>&1 || true
            fi
            mkdir -p "$WORK/latest"
            rm -rf "$WORK/latest/$JOB_ID"
            mkdir -p "$WORK/latest/$JOB_ID"
            cp -a "$SRC_DIR"/. "$WORK/latest/$JOB_ID/"
            git -C "$WORK" add -A
            if git -C "$WORK" diff --cached --quiet; then
              echo "No benchmark publish changes for $JOB_ID"
              cleanup_worktree
              exit 0
            fi
            git -C "$WORK" commit -m "chore: update benchmark results for ${JOB_ID}"
            if git -C "$WORK" push origin "HEAD:$PUBLISH_BRANCH"; then
              echo "Published $JOB_ID to $PUBLISH_BRANCH/latest/$JOB_ID"
              cleanup_worktree
              exit 0
            fi
            echo "Push conflict; retrying..."
            cleanup_worktree
            attempt=$((attempt + 1))
            sleep $((attempt * 2))
          done
          echo "Failed to publish after retries"
          exit 1
      - name: Gather vcpkg logs
        if: failure()
        run: |
          mkdir -p build/vcpkg-logs
          if [ -d /tmp/vcpkg/buildtrees ]; then
            find /tmp/vcpkg/buildtrees -name "*.log" | while IFS= read -r f; do
              rel="${f#/tmp/vcpkg/}"
              mkdir -p "build/vcpkg-logs/$(dirname "$rel")"
              cp "$f" "build/vcpkg-logs/$rel"
            done
          fi
      - name: Upload logs on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: std20-centos9-amd64-logs
          path: |
            build/CMakeFiles/CMakeOutput.log
            build/CMakeFiles/CMakeError.log
            build/Testing/Temporary/
            build/vcpkg-logs/
          if-no-files-found: ignore


  std20-centos9-arm64:
    if: ${{ inputs.enable-std20-centos9-arm64 }}
    runs-on: ubuntu-24.04-arm
    container: ghcr.io/koomx/kumo-std20-centos9-arm64:latest
    steps:
      - uses: actions/checkout@v4
      - name: Dump environment
        run: |
          echo "=== Environment Dump ==="
          if [ -f /etc/os-release ]; then
              . /etc/os-release
              echo "OS: $NAME $VERSION ($(uname -m))"
          elif [ "$(uname)" = "Darwin" ]; then
              echo "OS: macOS $(sw_vers -productVersion 2>/dev/null || echo unknown) ($(uname -m))"
          else
              echo "OS: $(uname -a 2>/dev/null || echo unknown)"
          fi
          for tool in cmake ninja make git python3 curl tar gzip; do
              if command -v $tool &>/dev/null; then
                  echo "  $tool: $(command -v $tool)  ($($tool --version 2>&1 | head -1))"
              else
                  echo "  $tool: NOT FOUND"
              fi
          done
          for cc in gcc g++ clang clang++ cc c++; do
              if command -v $cc &>/dev/null; then
                  echo "  $($cc --version 2>&1 | head -1)"
                  break
              fi
          done
          echo "=== End ==="
      - name: Install deps
        run: |
          if [ -n "${{ inputs.deps-centos }}" ]; then
            dnf install -y ${{ inputs.deps-centos }}
          fi
      - name: Bootstrap vcpkg
        run: |
          git clone --depth 1 --branch master https://github.com/microsoft/vcpkg.git /tmp/vcpkg
          /tmp/vcpkg/bootstrap-vcpkg.sh -disableMetrics
          echo "VCPKG_ROOT=/tmp/vcpkg" >> "$GITHUB_ENV"
          echo "VCPKG_CMAKE=/tmp/vcpkg/scripts/buildsystems/vcpkg.cmake" >> "$GITHUB_ENV"
          echo "VCPKG_FORCE_SYSTEM_BINARIES=1" >> "$GITHUB_ENV"
          mkdir -p "$RUNNER_TEMP/vcpkg-binary-cache"
          echo "VCPKG_DEFAULT_BINARY_CACHE=$RUNNER_TEMP/vcpkg-binary-cache" >> "$GITHUB_ENV"
      - name: Cache vcpkg binaries
        uses: actions/cache@v4
        with:
          path: ${{ runner.temp }}/vcpkg-binary-cache
          key: vcpkg-bin-std20-centos9-arm64-${{ hashFiles('vcpkg.json', 'vcpkg-configuration.json', 'vcpkg-lock.json') }}
          restore-keys: |
            vcpkg-bin-std20-centos9-arm64-
      - name: Configure
        run: ${{ inputs.config-command }}
      - name: Build
        run: ${{ inputs.build-command }}
      - name: Test
        if: ${{ inputs.test-command != '' }}
        run: ${{ inputs.test-command }}
      - name: Benchmark
        if: ${{ inputs.benchmark-command != '' }}
        run: ${{ inputs.benchmark-command }}
      - name: Plot
        if: ${{ inputs.plot-command != '' }}
        run: ${{ inputs.plot-command }}
      - name: Upload benchmark results
        if: ${{ (inputs.benchmark-command != '' || inputs.plot-command != '') && !cancelled() }}
        uses: actions/upload-artifact@v4
        with:
          name: std20-centos9-arm64-benchmarks
          path: ${{ inputs.benchmark-artifact-path }}
          if-no-files-found: ignore
      - name: Publish benchmark results
        if: ${{ inputs.benchmark-publish-branch != '' && (inputs.benchmark-publish-job == '' || inputs.benchmark-publish-job == 'std20-centos9-arm64') && success() && (github.event_name == 'push' || github.event_name == 'workflow_dispatch') }}
        shell: bash
        env:
          PUBLISH_BRANCH: ${{ inputs.benchmark-publish-branch }}
          SRC_DIR: ${{ inputs.benchmark-artifact-path }}
          GITHUB_TOKEN: ${{ github.token }}
          GITHUB_REPOSITORY: ${{ github.repository }}
        run: |
          set -euo pipefail
          if [ ! -d "$SRC_DIR" ]; then
            echo "No benchmark results at $SRC_DIR; skip publish"
            exit 0
          fi
          JOB_ID="${GITHUB_JOB}"
          ROOT="$GITHUB_WORKSPACE"
          cd "$ROOT"
          git config --global --add safe.directory "$ROOT"
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git remote set-url origin "https://x-access-token:${GITHUB_TOKEN}@github.com/${GITHUB_REPOSITORY}.git"
          WORK="$RUNNER_TEMP/bench-publish-$JOB_ID"
          cleanup_worktree() {
            cd "$ROOT"
            git worktree remove --force "$WORK" 2>/dev/null || true
            rm -rf "$WORK"
          }
          attempt=1
          while [ "$attempt" -le 12 ]; do
            echo "Publish attempt $attempt for $JOB_ID"
            cleanup_worktree
            mkdir -p "$WORK"
            if git fetch --depth 1 origin "$PUBLISH_BRANCH"; then
              git worktree add --force "$WORK" "FETCH_HEAD"
            else
              git worktree add --force --detach "$WORK"
              git -C "$WORK" checkout --orphan "$PUBLISH_BRANCH"
              git -C "$WORK" rm -rf . >/dev/null 2>&1 || true
            fi
            mkdir -p "$WORK/latest"
            rm -rf "$WORK/latest/$JOB_ID"
            mkdir -p "$WORK/latest/$JOB_ID"
            cp -a "$SRC_DIR"/. "$WORK/latest/$JOB_ID/"
            git -C "$WORK" add -A
            if git -C "$WORK" diff --cached --quiet; then
              echo "No benchmark publish changes for $JOB_ID"
              cleanup_worktree
              exit 0
            fi
            git -C "$WORK" commit -m "chore: update benchmark results for ${JOB_ID}"
            if git -C "$WORK" push origin "HEAD:$PUBLISH_BRANCH"; then
              echo "Published $JOB_ID to $PUBLISH_BRANCH/latest/$JOB_ID"
              cleanup_worktree
              exit 0
            fi
            echo "Push conflict; retrying..."
            cleanup_worktree
            attempt=$((attempt + 1))
            sleep $((attempt * 2))
          done
          echo "Failed to publish after retries"
          exit 1
      - name: Gather vcpkg logs
        if: failure()
        run: |
          mkdir -p build/vcpkg-logs
          if [ -d /tmp/vcpkg/buildtrees ]; then
            find /tmp/vcpkg/buildtrees -name "*.log" | while IFS= read -r f; do
              rel="${f#/tmp/vcpkg/}"
              mkdir -p "build/vcpkg-logs/$(dirname "$rel")"
              cp "$f" "build/vcpkg-logs/$rel"
            done
          fi
      - name: Upload logs on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: std20-centos9-arm64-logs
          path: |
            build/CMakeFiles/CMakeOutput.log
            build/CMakeFiles/CMakeError.log
            build/Testing/Temporary/
            build/vcpkg-logs/
          if-no-files-found: ignore


  macos-arm64:
    if: ${{ inputs.enable-macos-arm64 }}
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - name: Dump environment
        run: |
          echo "=== Environment Dump ==="
          if [ -f /etc/os-release ]; then
              . /etc/os-release
              echo "OS: $NAME $VERSION ($(uname -m))"
          elif [ "$(uname)" = "Darwin" ]; then
              echo "OS: macOS $(sw_vers -productVersion 2>/dev/null || echo unknown) ($(uname -m))"
          else
              echo "OS: $(uname -a 2>/dev/null || echo unknown)"
          fi
          for tool in cmake ninja make git python3 curl tar gzip; do
              if command -v $tool &>/dev/null; then
                  echo "  $tool: $(command -v $tool)  ($($tool --version 2>&1 | head -1))"
              else
                  echo "  $tool: NOT FOUND"
              fi
          done
          for cc in gcc g++ clang clang++ cc c++; do
              if command -v $cc &>/dev/null; then
                  echo "  $($cc --version 2>&1 | head -1)"
                  break
              fi
          done
          echo "=== End ==="
      - name: Install deps
        run: |
          brew install curl zip unzip cmake ninja autoconf automake libtool pkg-config git python3 ${{ inputs.deps-macos }}
      - name: Bootstrap vcpkg
        run: |
          git clone --depth 1 --branch master https://github.com/microsoft/vcpkg.git /tmp/vcpkg
          /tmp/vcpkg/bootstrap-vcpkg.sh -disableMetrics
          echo "VCPKG_ROOT=/tmp/vcpkg" >> $GITHUB_ENV
          echo "/tmp/vcpkg" >> $GITHUB_PATH
          echo "VCPKG_CMAKE=/tmp/vcpkg/scripts/buildsystems/vcpkg.cmake" >> $GITHUB_ENV
          mkdir -p "$RUNNER_TEMP/vcpkg-binary-cache"
          echo "VCPKG_DEFAULT_BINARY_CACHE=$RUNNER_TEMP/vcpkg-binary-cache" >> $GITHUB_ENV
      - name: Cache vcpkg binaries
        uses: actions/cache@v4
        with:
          path: ${{ runner.temp }}/vcpkg-binary-cache
          key: vcpkg-bin-macos-arm64-${{ hashFiles('vcpkg.json', 'vcpkg-configuration.json', 'vcpkg-lock.json') }}
          restore-keys: |
            vcpkg-bin-macos-arm64-
      - name: Configure
        run: ${{ inputs.config-command }}
      - name: Build
        run: |
          nproc() { sysctl -n hw.logicalcpu; }
          ${{ inputs.build-command }}
      - name: Test
        if: ${{ inputs.test-command != '' }}
        run: ${{ inputs.test-command }}
      - name: Benchmark
        if: ${{ inputs.benchmark-command != '' }}
        run: ${{ inputs.benchmark-command }}
      - name: Plot
        if: ${{ inputs.plot-command != '' }}
        run: ${{ inputs.plot-command }}
      - name: Upload benchmark results
        if: ${{ (inputs.benchmark-command != '' || inputs.plot-command != '') && !cancelled() }}
        uses: actions/upload-artifact@v4
        with:
          name: macos-arm64-benchmarks
          path: ${{ inputs.benchmark-artifact-path }}
          if-no-files-found: ignore
      - name: Publish benchmark results
        if: ${{ inputs.benchmark-publish-branch != '' && (inputs.benchmark-publish-job == '' || inputs.benchmark-publish-job == 'macos-arm64') && success() && (github.event_name == 'push' || github.event_name == 'workflow_dispatch') }}
        shell: bash
        env:
          PUBLISH_BRANCH: ${{ inputs.benchmark-publish-branch }}
          SRC_DIR: ${{ inputs.benchmark-artifact-path }}
          GITHUB_TOKEN: ${{ github.token }}
          GITHUB_REPOSITORY: ${{ github.repository }}
        run: |
          set -euo pipefail
          if [ ! -d "$SRC_DIR" ]; then
            echo "No benchmark results at $SRC_DIR; skip publish"
            exit 0
          fi
          JOB_ID="${GITHUB_JOB}"
          ROOT="$GITHUB_WORKSPACE"
          cd "$ROOT"
          git config --global --add safe.directory "$ROOT"
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git remote set-url origin "https://x-access-token:${GITHUB_TOKEN}@github.com/${GITHUB_REPOSITORY}.git"
          WORK="$RUNNER_TEMP/bench-publish-$JOB_ID"
          cleanup_worktree() {
            cd "$ROOT"
            git worktree remove --force "$WORK" 2>/dev/null || true
            rm -rf "$WORK"
          }
          attempt=1
          while [ "$attempt" -le 12 ]; do
            echo "Publish attempt $attempt for $JOB_ID"
            cleanup_worktree
            mkdir -p "$WORK"
            if git fetch --depth 1 origin "$PUBLISH_BRANCH"; then
              git worktree add --force "$WORK" "FETCH_HEAD"
            else
              git worktree add --force --detach "$WORK"
              git -C "$WORK" checkout --orphan "$PUBLISH_BRANCH"
              git -C "$WORK" rm -rf . >/dev/null 2>&1 || true
            fi
            mkdir -p "$WORK/latest"
            rm -rf "$WORK/latest/$JOB_ID"
            mkdir -p "$WORK/latest/$JOB_ID"
            cp -a "$SRC_DIR"/. "$WORK/latest/$JOB_ID/"
            git -C "$WORK" add -A
            if git -C "$WORK" diff --cached --quiet; then
              echo "No benchmark publish changes for $JOB_ID"
              cleanup_worktree
              exit 0
            fi
            git -C "$WORK" commit -m "chore: update benchmark results for ${JOB_ID}"
            if git -C "$WORK" push origin "HEAD:$PUBLISH_BRANCH"; then
              echo "Published $JOB_ID to $PUBLISH_BRANCH/latest/$JOB_ID"
              cleanup_worktree
              exit 0
            fi
            echo "Push conflict; retrying..."
            cleanup_worktree
            attempt=$((attempt + 1))
            sleep $((attempt * 2))
          done
          echo "Failed to publish after retries"
          exit 1
      - name: Gather vcpkg logs
        if: failure()
        run: |
          mkdir -p /tmp/ci-logs
          if [ -d /tmp/vcpkg/buildtrees ]; then
            find /tmp/vcpkg/buildtrees -name "*.log" | while IFS= read -r f; do
              rel="${f#/tmp/vcpkg/}"
              mkdir -p "/tmp/ci-logs/$(dirname "$rel")"
              cp "$f" "/tmp/ci-logs/$rel"
            done
          fi
      - name: Upload logs on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: macos-arm64-logs
          path: |
            build/CMakeFiles/CMakeOutput.log
            build/CMakeFiles/CMakeError.log
            build/Testing/Temporary/
            /tmp/ci-logs/
          if-no-files-found: ignore

  macos-x86:
    if: ${{ inputs.enable-macos-x86 }}
    runs-on: macos-13
    steps:
      - uses: actions/checkout@v4
      - name: Dump environment
        run: |
          echo "=== Environment Dump ==="
          if [ -f /etc/os-release ]; then
              . /etc/os-release
              echo "OS: $NAME $VERSION ($(uname -m))"
          elif [ "$(uname)" = "Darwin" ]; then
              echo "OS: macOS $(sw_vers -productVersion 2>/dev/null || echo unknown) ($(uname -m))"
          else
              echo "OS: $(uname -a 2>/dev/null || echo unknown)"
          fi
          for tool in cmake ninja make git python3 curl tar gzip; do
              if command -v $tool &>/dev/null; then
                  echo "  $tool: $(command -v $tool)  ($($tool --version 2>&1 | head -1))"
              else
                  echo "  $tool: NOT FOUND"
              fi
          done
          for cc in gcc g++ clang clang++ cc c++; do
              if command -v $cc &>/dev/null; then
                  echo "  $($cc --version 2>&1 | head -1)"
                  break
              fi
          done
          echo "=== End ==="
      - name: Install deps
        run: |
          brew install curl zip unzip cmake ninja autoconf automake libtool pkg-config git python3 ${{ inputs.deps-macos }}
      - name: Bootstrap vcpkg
        run: |
          git clone --depth 1 --branch master https://github.com/microsoft/vcpkg.git /tmp/vcpkg
          /tmp/vcpkg/bootstrap-vcpkg.sh -disableMetrics
          echo "VCPKG_ROOT=/tmp/vcpkg" >> $GITHUB_ENV
          echo "/tmp/vcpkg" >> $GITHUB_PATH
          echo "VCPKG_CMAKE=/tmp/vcpkg/scripts/buildsystems/vcpkg.cmake" >> $GITHUB_ENV
          mkdir -p "$RUNNER_TEMP/vcpkg-binary-cache"
          echo "VCPKG_DEFAULT_BINARY_CACHE=$RUNNER_TEMP/vcpkg-binary-cache" >> $GITHUB_ENV
      - name: Cache vcpkg binaries
        uses: actions/cache@v4
        with:
          path: ${{ runner.temp }}/vcpkg-binary-cache
          key: vcpkg-bin-macos-x86-${{ hashFiles('vcpkg.json', 'vcpkg-configuration.json', 'vcpkg-lock.json') }}
          restore-keys: |
            vcpkg-bin-macos-x86-
      - name: Configure
        run: ${{ inputs.config-command }}
      - name: Build
        run: |
          nproc() { sysctl -n hw.logicalcpu; }
          ${{ inputs.build-command }}
      - name: Test
        if: ${{ inputs.test-command != '' }}
        run: ${{ inputs.test-command }}
      - name: Benchmark
        if: ${{ inputs.benchmark-command != '' }}
        run: ${{ inputs.benchmark-command }}
      - name: Plot
        if: ${{ inputs.plot-command != '' }}
        run: ${{ inputs.plot-command }}
      - name: Upload benchmark results
        if: ${{ (inputs.benchmark-command != '' || inputs.plot-command != '') && !cancelled() }}
        uses: actions/upload-artifact@v4
        with:
          name: macos-x86-benchmarks
          path: ${{ inputs.benchmark-artifact-path }}
          if-no-files-found: ignore
      - name: Publish benchmark results
        if: ${{ inputs.benchmark-publish-branch != '' && (inputs.benchmark-publish-job == '' || inputs.benchmark-publish-job == 'macos-x86') && success() && (github.event_name == 'push' || github.event_name == 'workflow_dispatch') }}
        shell: bash
        env:
          PUBLISH_BRANCH: ${{ inputs.benchmark-publish-branch }}
          SRC_DIR: ${{ inputs.benchmark-artifact-path }}
          GITHUB_TOKEN: ${{ github.token }}
          GITHUB_REPOSITORY: ${{ github.repository }}
        run: |
          set -euo pipefail
          if [ ! -d "$SRC_DIR" ]; then
            echo "No benchmark results at $SRC_DIR; skip publish"
            exit 0
          fi
          JOB_ID="${GITHUB_JOB}"
          ROOT="$GITHUB_WORKSPACE"
          cd "$ROOT"
          git config --global --add safe.directory "$ROOT"
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git remote set-url origin "https://x-access-token:${GITHUB_TOKEN}@github.com/${GITHUB_REPOSITORY}.git"
          WORK="$RUNNER_TEMP/bench-publish-$JOB_ID"
          cleanup_worktree() {
            cd "$ROOT"
            git worktree remove --force "$WORK" 2>/dev/null || true
            rm -rf "$WORK"
          }
          attempt=1
          while [ "$attempt" -le 12 ]; do
            echo "Publish attempt $attempt for $JOB_ID"
            cleanup_worktree
            mkdir -p "$WORK"
            if git fetch --depth 1 origin "$PUBLISH_BRANCH"; then
              git worktree add --force "$WORK" "FETCH_HEAD"
            else
              git worktree add --force --detach "$WORK"
              git -C "$WORK" checkout --orphan "$PUBLISH_BRANCH"
              git -C "$WORK" rm -rf . >/dev/null 2>&1 || true
            fi
            mkdir -p "$WORK/latest"
            rm -rf "$WORK/latest/$JOB_ID"
            mkdir -p "$WORK/latest/$JOB_ID"
            cp -a "$SRC_DIR"/. "$WORK/latest/$JOB_ID/"
            git -C "$WORK" add -A
            if git -C "$WORK" diff --cached --quiet; then
              echo "No benchmark publish changes for $JOB_ID"
              cleanup_worktree
              exit 0
            fi
            git -C "$WORK" commit -m "chore: update benchmark results for ${JOB_ID}"
            if git -C "$WORK" push origin "HEAD:$PUBLISH_BRANCH"; then
              echo "Published $JOB_ID to $PUBLISH_BRANCH/latest/$JOB_ID"
              cleanup_worktree
              exit 0
            fi
            echo "Push conflict; retrying..."
            cleanup_worktree
            attempt=$((attempt + 1))
            sleep $((attempt * 2))
          done
          echo "Failed to publish after retries"
          exit 1
      - name: Gather vcpkg logs
        if: failure()
        run: |
          mkdir -p /tmp/ci-logs
          if [ -d /tmp/vcpkg/buildtrees ]; then
            find /tmp/vcpkg/buildtrees -name "*.log" | while IFS= read -r f; do
              rel="${f#/tmp/vcpkg/}"
              mkdir -p "/tmp/ci-logs/$(dirname "$rel")"
              cp "$f" "/tmp/ci-logs/$rel"
            done
          fi
      - name: Upload logs on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: macos-x86-logs
          path: |
            build/CMakeFiles/CMakeOutput.log
            build/CMakeFiles/CMakeError.log
            build/Testing/Temporary/
            /tmp/ci-logs/
          if-no-files-found: ignore

  windows:
    if: ${{ inputs.enable-windows }}
    runs-on: windows-latest
    defaults:
      run:
        shell: pwsh
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Dump environment
        run: |
          Write-Host "=== Environment Dump ==="
          Write-Host "OS: $((Get-CimInstance Win32_OperatingSystem).Caption) $((Get-CimInstance Win32_OperatingSystem).OSArchitecture)"
          $tools = @('cmake', 'ninja', 'git', 'python3', 'curl', 'tar')
          foreach ($tool in $tools) {
              $path = Get-Command $tool -ErrorAction SilentlyContinue
              if ($path) {
                  $ver = & $tool --version 2>&1 | Select-Object -First 1
                  Write-Host "  ${tool}: $($path.Source)  ($ver)"
              } else {
                  Write-Host "  ${tool}: NOT FOUND"
              }
          }
          Write-Host "=== End ==="
      - name: Install deps
        run: |
          choco install openssl strawberryperl ${{ inputs.deps-windows }} -y
      - name: Bootstrap vcpkg
        env:
          TEMP_DIR: ${{ runner.temp }}
        run: |
          $vcpkgRoot = Join-Path $env:TEMP_DIR "vcpkg"
          git clone --depth 1 --branch master https://github.com/microsoft/vcpkg.git "$vcpkgRoot"
          & "$vcpkgRoot/bootstrap-vcpkg.bat" -disableMetrics
          echo "VCPKG_ROOT=$vcpkgRoot" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append
          echo "$vcpkgRoot" | Out-File -FilePath $env:GITHUB_PATH -Encoding utf8 -Append
          echo "VCPKG_CMAKE=$vcpkgRoot/scripts/buildsystems/vcpkg.cmake" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append
          echo "VCPKG_FORCE_SYSTEM_BINARIES=1" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append
          echo "VCPKG_KEEP_ENV_VARS=PATH" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append
          $cacheDir = Join-Path $env:TEMP_DIR "vcpkg-binary-cache"
          New-Item -ItemType Directory -Path $cacheDir -Force | Out-Null
          echo "VCPKG_DEFAULT_BINARY_CACHE=$cacheDir" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append
      - name: Cache vcpkg binaries
        uses: actions/cache@v4
        with:
          path: ${{ runner.temp }}/vcpkg-binary-cache
          key: vcpkg-bin-windows-x64-${{ hashFiles('vcpkg.json', 'vcpkg-configuration.json', 'vcpkg-lock.json') }}
          restore-keys: |
            vcpkg-bin-windows-x64-
      - name: Configure
        run: ${{ inputs.config-command }}
      - name: Build
        run: |
          if (-not (Get-Command nproc -ErrorAction SilentlyContinue)) {
            function global:nproc { [Environment]::ProcessorCount }
          }
          ${{ inputs.build-command }}
      - name: Test
        if: ${{ inputs.test-command != '' }}
        run: ${{ inputs.test-command }}
      - name: Benchmark
        if: ${{ inputs.benchmark-command != '' }}
        run: ${{ inputs.benchmark-command }}
      - name: Plot
        if: ${{ inputs.plot-command != '' }}
        run: ${{ inputs.plot-command }}
      - name: Upload benchmark results
        if: ${{ (inputs.benchmark-command != '' || inputs.plot-command != '') && !cancelled() }}
        uses: actions/upload-artifact@v4
        with:
          name: windows-benchmarks
          path: ${{ inputs.benchmark-artifact-path }}
          if-no-files-found: ignore
      - name: Publish benchmark results
        if: ${{ inputs.benchmark-publish-branch != '' && (inputs.benchmark-publish-job == '' || inputs.benchmark-publish-job == 'windows') && success() && (github.event_name == 'push' || github.event_name == 'workflow_dispatch') }}
        shell: bash
        env:
          PUBLISH_BRANCH: ${{ inputs.benchmark-publish-branch }}
          SRC_DIR: ${{ inputs.benchmark-artifact-path }}
          GITHUB_TOKEN: ${{ github.token }}
          GITHUB_REPOSITORY: ${{ github.repository }}
        run: |
          set -euo pipefail
          if [ ! -d "$SRC_DIR" ]; then
            echo "No benchmark results at $SRC_DIR; skip publish"
            exit 0
          fi
          JOB_ID="${GITHUB_JOB}"
          ROOT="$GITHUB_WORKSPACE"
          cd "$ROOT"
          git config --global --add safe.directory "$ROOT"
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git remote set-url origin "https://x-access-token:${GITHUB_TOKEN}@github.com/${GITHUB_REPOSITORY}.git"
          WORK="$RUNNER_TEMP/bench-publish-$JOB_ID"
          cleanup_worktree() {
            cd "$ROOT"
            git worktree remove --force "$WORK" 2>/dev/null || true
            rm -rf "$WORK"
          }
          attempt=1
          while [ "$attempt" -le 12 ]; do
            echo "Publish attempt $attempt for $JOB_ID"
            cleanup_worktree
            mkdir -p "$WORK"
            if git fetch --depth 1 origin "$PUBLISH_BRANCH"; then
              git worktree add --force "$WORK" "FETCH_HEAD"
            else
              git worktree add --force --detach "$WORK"
              git -C "$WORK" checkout --orphan "$PUBLISH_BRANCH"
              git -C "$WORK" rm -rf . >/dev/null 2>&1 || true
            fi
            mkdir -p "$WORK/latest"
            rm -rf "$WORK/latest/$JOB_ID"
            mkdir -p "$WORK/latest/$JOB_ID"
            cp -a "$SRC_DIR"/. "$WORK/latest/$JOB_ID/"
            git -C "$WORK" add -A
            if git -C "$WORK" diff --cached --quiet; then
              echo "No benchmark publish changes for $JOB_ID"
              cleanup_worktree
              exit 0
            fi
            git -C "$WORK" commit -m "chore: update benchmark results for ${JOB_ID}"
            if git -C "$WORK" push origin "HEAD:$PUBLISH_BRANCH"; then
              echo "Published $JOB_ID to $PUBLISH_BRANCH/latest/$JOB_ID"
              cleanup_worktree
              exit 0
            fi
            echo "Push conflict; retrying..."
            cleanup_worktree
            attempt=$((attempt + 1))
            sleep $((attempt * 2))
          done
          echo "Failed to publish after retries"
          exit 1
      - name: Gather vcpkg logs
        if: failure()
        run: |
          $vcpkgRoot = $env:VCPKG_ROOT
          if (-not $vcpkgRoot) { return }
          $logDir = Join-Path $env:RUNNER_TEMP "ci-logs"
          $null = New-Item -ItemType Directory -Path $logDir -Force
          $buildtrees = Join-Path $vcpkgRoot "buildtrees"
          if (Test-Path $buildtrees) {
            Get-ChildItem -Path $buildtrees -Recurse -Filter *.log | ForEach-Object {
              $rel = $_.FullName.Substring($vcpkgRoot.Length + 1)
              $dest = Join-Path $logDir $rel
              $null = New-Item -ItemType Directory -Path (Split-Path $dest -Parent) -Force
              Copy-Item $_.FullName $dest
            }
          }
      - name: Upload logs on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: windows-logs
          path: |
            build/CMakeFiles/CMakeOutput.log
            build/CMakeFiles/CMakeError.log
            build/Testing/Temporary/
            ${{ runner.temp }}/ci-logs/
          if-no-files-found: ignore

  windows-arm64:
    if: ${{ inputs.enable-windows-arm64 }}
    runs-on: windows-11-arm
    defaults:
      run:
        shell: pwsh
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Dump environment
        run: |
          Write-Host "=== Environment Dump ==="
          Write-Host "OS: $((Get-CimInstance Win32_OperatingSystem).Caption) $((Get-CimInstance Win32_OperatingSystem).OSArchitecture)"
          $tools = @('cmake', 'ninja', 'git', 'python3', 'curl', 'tar')
          foreach ($tool in $tools) {
              $path = Get-Command $tool -ErrorAction SilentlyContinue
              if ($path) {
                  $ver = & $tool --version 2>&1 | Select-Object -First 1
                  Write-Host "  ${tool}: $($path.Source)  ($ver)"
              } else {
                  Write-Host "  ${tool}: NOT FOUND"
              }
          }
          Write-Host "=== End ==="
      - name: Install deps
        run: |
          choco install openssl strawberryperl ${{ inputs.deps-windows }} -y
      - name: Bootstrap vcpkg
        env:
          TEMP_DIR: ${{ runner.temp }}
        run: |
          $vcpkgRoot = Join-Path $env:TEMP_DIR "vcpkg"
          git clone --depth 1 --branch master https://github.com/microsoft/vcpkg.git "$vcpkgRoot"
          & "$vcpkgRoot/bootstrap-vcpkg.bat" -disableMetrics
          echo "VCPKG_ROOT=$vcpkgRoot" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append
          echo "$vcpkgRoot" | Out-File -FilePath $env:GITHUB_PATH -Encoding utf8 -Append
          echo "VCPKG_CMAKE=$vcpkgRoot/scripts/buildsystems/vcpkg.cmake" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append
          echo "VCPKG_FORCE_SYSTEM_BINARIES=1" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append
          echo "VCPKG_KEEP_ENV_VARS=PATH" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append
          $cacheDir = Join-Path $env:TEMP_DIR "vcpkg-binary-cache"
          New-Item -ItemType Directory -Path $cacheDir -Force | Out-Null
          echo "VCPKG_DEFAULT_BINARY_CACHE=$cacheDir" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append
      - name: Cache vcpkg binaries
        uses: actions/cache@v4
        with:
          path: ${{ runner.temp }}/vcpkg-binary-cache
          key: vcpkg-bin-windows-arm64-${{ hashFiles('vcpkg.json', 'vcpkg-configuration.json', 'vcpkg-lock.json') }}
          restore-keys: |
            vcpkg-bin-windows-arm64-
      - name: Configure
        run: ${{ inputs.config-command }}
      - name: Build
        run: |
          if (-not (Get-Command nproc -ErrorAction SilentlyContinue)) {
            function global:nproc { [Environment]::ProcessorCount }
          }
          ${{ inputs.build-command }}
      - name: Test
        if: ${{ inputs.test-command != '' }}
        run: ${{ inputs.test-command }}
      - name: Benchmark
        if: ${{ inputs.benchmark-command != '' }}
        run: ${{ inputs.benchmark-command }}
      - name: Plot
        if: ${{ inputs.plot-command != '' }}
        run: ${{ inputs.plot-command }}
      - name: Upload benchmark results
        if: ${{ (inputs.benchmark-command != '' || inputs.plot-command != '') && !cancelled() }}
        uses: actions/upload-artifact@v4
        with:
          name: windows-arm64-benchmarks
          path: ${{ inputs.benchmark-artifact-path }}
          if-no-files-found: ignore
      - name: Publish benchmark results
        if: ${{ inputs.benchmark-publish-branch != '' && (inputs.benchmark-publish-job == '' || inputs.benchmark-publish-job == 'windows-arm64') && success() && (github.event_name == 'push' || github.event_name == 'workflow_dispatch') }}
        shell: bash
        env:
          PUBLISH_BRANCH: ${{ inputs.benchmark-publish-branch }}
          SRC_DIR: ${{ inputs.benchmark-artifact-path }}
          GITHUB_TOKEN: ${{ github.token }}
          GITHUB_REPOSITORY: ${{ github.repository }}
        run: |
          set -euo pipefail
          if [ ! -d "$SRC_DIR" ]; then
            echo "No benchmark results at $SRC_DIR; skip publish"
            exit 0
          fi
          JOB_ID="${GITHUB_JOB}"
          ROOT="$GITHUB_WORKSPACE"
          cd "$ROOT"
          git config --global --add safe.directory "$ROOT"
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git remote set-url origin "https://x-access-token:${GITHUB_TOKEN}@github.com/${GITHUB_REPOSITORY}.git"
          WORK="$RUNNER_TEMP/bench-publish-$JOB_ID"
          cleanup_worktree() {
            cd "$ROOT"
            git worktree remove --force "$WORK" 2>/dev/null || true
            rm -rf "$WORK"
          }
          attempt=1
          while [ "$attempt" -le 12 ]; do
            echo "Publish attempt $attempt for $JOB_ID"
            cleanup_worktree
            mkdir -p "$WORK"
            if git fetch --depth 1 origin "$PUBLISH_BRANCH"; then
              git worktree add --force "$WORK" "FETCH_HEAD"
            else
              git worktree add --force --detach "$WORK"
              git -C "$WORK" checkout --orphan "$PUBLISH_BRANCH"
              git -C "$WORK" rm -rf . >/dev/null 2>&1 || true
            fi
            mkdir -p "$WORK/latest"
            rm -rf "$WORK/latest/$JOB_ID"
            mkdir -p "$WORK/latest/$JOB_ID"
            cp -a "$SRC_DIR"/. "$WORK/latest/$JOB_ID/"
            git -C "$WORK" add -A
            if git -C "$WORK" diff --cached --quiet; then
              echo "No benchmark publish changes for $JOB_ID"
              cleanup_worktree
              exit 0
            fi
            git -C "$WORK" commit -m "chore: update benchmark results for ${JOB_ID}"
            if git -C "$WORK" push origin "HEAD:$PUBLISH_BRANCH"; then
              echo "Published $JOB_ID to $PUBLISH_BRANCH/latest/$JOB_ID"
              cleanup_worktree
              exit 0
            fi
            echo "Push conflict; retrying..."
            cleanup_worktree
            attempt=$((attempt + 1))
            sleep $((attempt * 2))
          done
          echo "Failed to publish after retries"
          exit 1
      - name: Gather vcpkg logs
        if: failure()
        run: |
          $vcpkgRoot = $env:VCPKG_ROOT
          if (-not $vcpkgRoot) { return }
          $logDir = Join-Path $env:RUNNER_TEMP "ci-logs"
          $null = New-Item -ItemType Directory -Path $logDir -Force
          $buildtrees = Join-Path $vcpkgRoot "buildtrees"
          if (Test-Path $buildtrees) {
            Get-ChildItem -Path $buildtrees -Recurse -Filter *.log | ForEach-Object {
              $rel = $_.FullName.Substring($vcpkgRoot.Length + 1)
              $dest = Join-Path $logDir $rel
              $null = New-Item -ItemType Directory -Path (Split-Path $dest -Parent) -Force
              Copy-Item $_.FullName $dest
            }
          }
      - name: Upload logs on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: windows-arm64-logs
          path: |
            build/CMakeFiles/CMakeOutput.log
            build/CMakeFiles/CMakeError.log
            build/Testing/Temporary/
            ${{ runner.temp }}/ci-logs/
          if-no-files-found: ignore
